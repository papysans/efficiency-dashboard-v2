# AI Coding 指标看板 - 系统设计文档

## 一、项目背景

本项目是一个 AI Coding 指标看板程序，用于统计和分析 AI 辅助编程的使用情况。整体设计参考并复用自 `D:\My\PubCode\comdigger` 中的已有代码。
**如下所有的示例，逻辑，表格格式等，都是AI创建的，都是可以修改的，不一定以AI创建的为准，而应该深度思考**

### 1.1 业务场景
- 用户使用 IDE 插件（如 VSCode）与 AI 进行对话
- AI 根据用户输入生成代码（写入文件或应用 diff）
- 需要统计：用户使用时长、生成代码量、API 调用成本等指标
- 支持多维度分析：项目维度、用户维度、组织维度（org1-org4）

### 1.2 核心需求
- **数据采集**：解析 IDE 产生的日志文件，提取关键指标
- **数据存储**：按天分区存储到 Elasticsearch
- **数据聚合**：多维度归并（项目、仓库、用户、组织层级）
- **AI 估时**：分析 Task 内容，AI 估算需求人天
- **提效分析**（核心）：对比 AI 预估人天 vs 实际编码耗时，计算提效比例
- **可视化**：提供看板展示各类指标和提效分析报表，你必须要用skills playwright等好好测试一下UI。

---

## 二、系统约束

### 2.1 数据源约束
- 原始数据存储在 `rawdata/YYYY-MM/DD/用户UUID/*.json`
- 每个 JSON 文件是一次 AI 请求的完整记录
- 文件名格式：`YYYYMMDD-HHMMSS_requestID_userUUID_随机数.json`
- **注意**：需要从 request 表中能反向恢复原始文件路径

### 2.2 时间粒度约束
- 按天为粒度处理数据（索引名后缀为 YYYYMMDD）
- 历史数据（2 月以前）按月归并，删除按天归并的索引
- 数据时间字段统一使用 UTC 格式存储

### 2.3 聚合约束
- 只统计 `caller="chat"` 的记录
- 处理时长计算：相邻请求间隔 ≤10 分钟则合并，否则视为中断
- 组织信息（org1-org4）可能为空，需要兼容

### 2.4 组织信息约束

**数据来源与加载：**
- 从 CSV 文件导入，不配在 YAML 中
- CSV 路径通过 `config.yaml` 的 `org_csv_file` 指定
- 启动时加载到内存，提供 O(1) 查询性能
- 支持热更新：`kbcli reload-org` 或重启服务

**CSV 格式（标准字段）：**
```csv
user_id,user_name,org1,org2,org3,org4,git_user_id,git_user_name
fa50dba2-91ad-485e-92b0-483deee60d24,dengbinbox,示例公司,研发体系,平台部,基础设施组,dengbin,dengbinbox
a1b2c3d4-e5f6-7890-abcd-ef1234567890,zhangsan,示例公司,研发体系,后端组,支付小组,zhangsan,zhangsan
,wangwu,示例公司,产品体系,产品一部,用户产品组,wangwu,wangwu1990
```

**字段说明：**
| 字段 | 说明 | 用途 |
|------|------|------|
| `user_id` | 系统用户 UUID | 与 request/task 表关联 |
| `user_name` | 系统用户名 | 备用匹配、展示 |
| `org1-org4` | 组织架构 | 组织维度分析 |
| `git_user_id` | Git 用户 ID（如 GitHub ID） | Git 提交关联 |
| `git_user_name` | Git 提交用户名 | Git 提交关联 |

**Git 用户关联说明：**
- Git 提交者通过 `git_user_id` 或 `git_user_name` 与系统用户关联
- 关联成功后，可将 Git commit 归属于具体的 project/repo/user
- 支持多对多：一个 Git 用户可能对应多个系统用户（跨项目）

**匹配逻辑（优先级降序）：**
1. 按 `user_id` 精确匹配
2. 如果未匹配到，按 `user_name` 精确匹配
3. 如果都未匹配到，org1-org4 返回空字符串

**实现伪代码：**
```
GetOrgInfo(user_id, user_name):
    // 1. 优先按 user_id 匹配
    if user_id != "" && user_id 在 user_id_map 中:
        return user_id_map[user_id]
    
    // 2. 按 user_name 匹配
    if user_name != "" && user_name 在 user_name_map 中:
        return user_name_map[user_name]
    
    // 3. 未匹配到，返回空
    return {org1: "", org2: "", org3: "", org4: ""}
```

**内存数据结构：**
```go
type OrgProvider struct {
    userIDMap   map[string]OrgInfo   // key: user_id
    userNameMap map[string]OrgInfo   // key: user_name
    csvFile     string               // CSV 文件路径
}
```

### 2.6 成本计算约束
- 根据模型和 token 使用量计算 API 成本
- 公式：`cost = (inTokens / 1e6) * inPrice + (outTokens / 1e6) * outPrice`
- 模型价格可配置

### 2.7 命名约束
- 字段命名使用 snake_case
- 索引命名：`costrict_chat_{layer}_{date}` 或 `costrict_chat_{layer}_{dimension}_{date}`
- 层：request（原始请求）/ task（任务聚合）
- 维度：project / repo / user / org1 / org2 / org3 / org4（用于实时聚合，不建表）

---

## 三、数据架构设计

### 3.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  应用层 (Frontend + Backend)                                  │
│  - Dashboard：原始数据和统计表格展示                          │
│  - Project Panel：项目维度下钻分析（实时聚合）                │
│  - Repo Panel：git仓库维度下钻分析（实时聚合）                │
│  - User Panel：用户维度分析（实时聚合）                       │
│  - Org Panel：组织维度层级分析（实时聚合）                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              │   实时聚合查询（ES Aggregation）
                              │   按 project_id / user_id / org 等维度
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  任务层 (Task) - Elasticsearch                               │
│  costrict_chat_task_{YYYYMMDD}                               │
│  按 task_id 归并，包含 AI 估时                                 │
│  数据来源：从 Request 层聚合                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  原始层 (Request) - Elasticsearch                            │
│  costrict_chat_request_{YYYYMMDD}                            │
│  每个请求一条记录，解析自 JSON 日志                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  数据源 (RawData) - 文件系统                                 │
│  rawdata/YYYY-MM/DD/用户UUID/*.json                          │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 索引命名规范

只保留两个物理存储层，维度聚合通过实时查询实现。

| 层级 | 索引名称模板 | 说明 | 数据来源 |
|------|-------------|------|----------|
| Request | `costrict_chat_request_{YYYYMMDD}` | 原始请求记录，一天一个索引 | rawdata 文件 |
| Task | `costrict_chat_task_{YYYYMMDD}` | 任务维度聚合，含 AI 估时 | Request 表按 task_id 归并 |

**维度聚合（实时查询，不建表）：**

| 维度 | 聚合键 | 聚合方法 |
|------|--------|----------|
| project | `task.project_id` | ES terms aggregation |
| repo | `task.repo_id` | ES terms aggregation |
| user | `task.user_id` | ES terms aggregation |
| org1 | `task.org1` | ES terms aggregation |
| org2 | `task.org1_org2` | ES terms aggregation |
| org3 | `task.org1_org2_org3` | ES terms aggregation |
| org4 | `task.org1_org2_org3_org4` | ES terms aggregation |

---

## 四、数据结构定义

### 4.1 Request 层（costrict_chat_request_{YYYYMMDD}）

原始请求记录，每个 AI API 调用产生一条记录。

```json
{
  "@timestamp": "2026-03-09T06:16:51Z",
  "caller": "chat",
  "sender": "user",
  "task_id": "019d4288-3775-75e0-8672-9a80f01bb988",
  "request_id": "019d4288-88b6-735b-ab94-3dd61b39dc48",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "user_id": "fa50dba2-91ad-485e-92b0-483deee60d24",
  "user_name": "dengbinbox",
  "repo_id": "https://github.com/zgsm-ai/costrict.git",
  "project_path": "d:\\project\\costrict-all\\user-indicator-collection",
  "project_id": "7fc2b9a3bd:d:\\project\\costrict-all\\user-indicator-collection",
  "client_ide": "vscode",
  "client_version": "2.5.3",
  "client_os": "Windows",
  "prompt_mode": "vibe",
  "mode": "code",
  "model": "GLM-4.7",
  "user_in_chars": 44,
  "assistant_out_code_lines": 42,
  "system_tokens": 4110,
  "user_tokens": 371,
  "api_request_time": "2026-03-09T14:16:48+08:00",
  "api_end_time": "2026-03-09T14:16:51+08:00",
  "api_process_time": 8507,
  "api_ttft": 8422,
  "api_in_tokens": 5561,
  "api_out_tokens": 443,
  "api_cost": 4.45,
  "org1": "公司名",
  "org2": "体系名",
  "org3": "部门名",
  "org4": "团队名",
  "source_path": "request/2026-03/09/用户UUID/文件名.json"
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `@timestamp` | date | 请求完成时间（UTC） |
| `caller` | keyword | 调用来源，固定为 "chat" |
| `sender` | keyword | 发送者：user/system/tool |
| `task_id` | keyword | 任务唯一标识 |
| `request_id` | keyword | 请求唯一标识 |
| `client_id` | keyword | 客户端标识 |
| `user_id` | keyword | 用户 UUID |
| `user_name` | keyword | 用户名（fallback: name→phone→github_name→user_id） |
| `repo_id` | keyword | 仓库地址（如果有） |
| `project_path` | keyword | 项目本地路径 |
| `project_id` | keyword | 项目唯一标识（repo 或 client_id前10位+project_path） |
| `client_ide` | keyword | IDE 类型：vscode/jetbrains |
| `client_version` | keyword | 客户端版本 |
| `client_os` | keyword | 操作系统 |
| `prompt_mode` | keyword | 提示模式：vibe/normal |
| `mode` | keyword | 模式：code/chat |
| `model` | keyword | AI 模型名称 |
| `user_in_chars` | long | 用户输入字符数（中文=2，英文=1） |
| `assistant_out_code_lines` | long | AI 生成代码行数 |
| `system_tokens` | long | 系统 prompt token 数 |
| `user_tokens` | long | 用户输入 token 数 |
| `api_request_time` | date | API 请求开始时间 |
| `api_end_time` | date | API 请求结束时间 |
| `api_process_time` | long | API 总耗时（毫秒） |
| `api_ttft` | long | 首 token 延迟（毫秒） |
| `api_in_tokens` | long | API 输入 token 数 |
| `api_out_tokens` | long | API 输出 token 数 |
| `api_cost` | float | API 调用成本（元） |
| `org1` | keyword | 组织一级：公司/子公司 |
| `org2` | keyword | 组织二级：体系/BG |
| `org3` | keyword | 组织三级：部门 |
| `org4` | keyword | 组织四级：团队 |
| `source_path` | keyword | 原始文件路径（用于反向查找） |

### 4.2 Task 层（costrict_chat_task_{YYYYMMDD}）

任务维度聚合，从 Request 层按 task_id 归并而来。包含 AI 估算的需求人天。

```json
{
  "@timestamp": "2026-03-09T06:16:51Z",
  "task_id": "019d4288-3775-75e0-8672-9a80f01bb988",
  "caller": "chat",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "user_id": "fa50dba2-91ad-485e-92b0-483deee60d24",
  "user_name": "dengbinbox",
  "repo_id": "https://github.com/zgsm-ai/costrict.git",
  "project_path": "d:\\project\\costrict-all\\user-indicator-collection",
  "project_id": "7fc2b9a3bd:d:\\project\\costrict-all\\user-indicator-collection",
  "client_ide": "vscode",
  "client_version": "2.5.3",
  "client_os": "Windows",
  "prompt_mode": "vibe",
  "mode": "code",
  "org1": "公司名",
  "org2": "体系名",
  "org3": "部门名",
  "org4": "团队名",
  "user_in_chars": 440,
  "assistant_out_code_lines": 420,
  "system_tokens": 4110,
  "user_tokens": 371,
  "api_request_time": "2026-03-09T14:16:48+08:00",
  "api_end_time": "2026-03-09T14:16:51+08:00",
  "api_process_time": 85070,
  "api_ttft": 84220,
  "api_in_tokens": 55610,
  "api_out_tokens": 4430,
  "api_cost": 44.5,
  "api_count": 10,
  "source_file": "task/2026-03/09/019d4288-3775-75e0-8672-9a80f01bb988.json",
  "ai_estimated_days": 4.5,
  "ai_estimated_reason": "根据用户输入分析，该任务涉及 xx 功能实现，预计需要 4.5 人天"
}
```

**字段说明（继承 Request 层，并添加以下字段）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `api_count` | long | task 下的 request 数量 |
| `source_file` | keyword | task 内容文件路径，指向提取的用户输入和 AI 输出 |
| `ai_estimated_days` | float | AI 估算的需求人天 |
| `ai_estimated_reason` | text | 估算理由 |

**Task 层聚合逻辑：**
- 从 Request 层按 task_id 分组
- 数值字段求和（user_in_chars, assistant_out_code_lines, api_in_tokens 等）
- 时间字段取最早/最晚（api_request_time 取最早，api_end_time 取最晚）
- 提取用户输入和 AI 输出内容，生成 task 内容文件
- 调用 AI API 分析 task 内容，生成 ai_estimated_days 和 ai_estimated_reason

---

## 五、数据处理流程

整体流程：**rawdata → Request → Task**

> **注意**：不再创建 Stat 层物理表，维度聚合（project/user/org 等）改为实时从 Task 表查询（ES Aggregation）。

### 5.1 原始数据解析（kbcli reindex --step=request）

解析 rawdata 文件，生成 Request 层数据。

```
┌─────────────────┐     ┌──────────────┐     ┌───────────────────┐
│  rawdata 目录    │ ──▶ │  JSON 解析   │ ──▶ │  RequestDoc 结构  │
│  YYYY-MM/DD/    │     │ParseRequest  │     │                   │
│  *.json 文件    │     │              │     │                   │
└─────────────────┘     └──────────────┘     └─────────┬─────────┘
                                                       │
                               ┌───────────────────────┼───────────────────────┐
                               ▼                       ▼                       ▼
                         ┌──────────┐          ┌────────────┐          ┌────────────┐
                         │ 写入 ES  │          │ 组织信息   │          │ 成本计算   │
                         │ request  │          │ GetOrgInfo │          │ calculate  │
                         │ 索引     │          │            │          │   Cost     │
                         └──────────┘          └────────────┘          └────────────┘
```

**解析逻辑：**

1. **用户名字段（user_name）**：
   - 优先取 `identity.user_info.name`
   - 为空则取 `phone`
   - 为空则取 `github_name`
   - 为空则取 `identity.user_name`

2. **项目 ID（project_id）**：
   - 如果有 `repo_id`，则使用 `repo_id`
   - 否则使用 `client_id` 前 10 位 + `:` + `project_path`

3. **用户输入字符数（user_in_chars）**：
   - 条件：`sender == "user"`
   - 取 `llm_params.messages[-1].content`
   - 如果以 `<user_message>` 开头，提取标签内内容
   - 中文按 2 字符计算，英文按 1 字符计算

4. **代码行数（assistant_out_code_lines）**：
   - 遍历 `response_content.tool_calls`
   - 过滤 `function.name` 为 `write_to_file` 或 `apply_diff` 的记录
   - 提取 `arguments.content`，按换行符分割计算行数

5. **API 成本（api_cost）**：
   - 根据 `model` 从配置中读取价格
   - 公式：`(api_in_tokens / 1e6) * in_price + (api_out_tokens / 1e6) * out_price`

6. **组织信息（org1-org4）**：
   - 通过 `GetOrgInfo(user_id, user_name)` 查询
   - 从 CSV 文件导入，支持按 user_id 或 user_name 匹配
   - 匹配优先级：user_id > user_name

### 5.2 Task 聚合流程（kbcli reindex --step=task）

从 Request 层按 task_id 归并，生成 Task 层数据。

```
┌─────────────────────────────────┐
│ 从 Request 表读取数据           │
│ costrict_chat_request_{date}    │
└────────┬────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ 按 task_id 分组                                           │
│ map[task_id][]RequestDoc                                  │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ 对每个 task_id 聚合：                                      │
│ 1. sum: user_in_chars, assistant_out_code_lines            │
│ 2. sum: system_tokens, user_tokens                         │
│ 3. sum: api_process_time, api_ttft                         │
│ 4. sum: api_in_tokens, api_out_tokens, api_cost            │
│ 5. min: api_request_time → api_request_time                │
│ 6. max: api_end_time → api_end_time                        │
│ 7. count: 请求次数 → 生成 api_count 字段                   │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ 提取用户输入和 AI 输出                                    │
│ 1. 遍历该 task 的所有 request 记录                         │
│ 2. user_in_chars > 0 → 提取 <user_message> 内容            │
│ 3. assistant_out_code_lines > 0 → 提取代码内容             │
│ 4. 生成类似 task/2026-03/09/$task_id.json 文件                            │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ AI 估时分析                                               │
│ - 输入：$task_id.json（用户输入 + AI 输出）           │
│ - 调用 LLM API 分析需求复杂度                              │
│ - 输出：ai_estimated_days, ai_estimated_reason            │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ 写入 Task 索引                                            │
│ costrict_chat_task_{YYYYMMDD}                           │
└──────────────────────────────────────────────────────────┘
```

**详细提取逻辑：**

**1. 提取用户输入内容（user_input）**

```
输入：单个 request 的 JSON 数据
条件：request.user_in_chars > 0
步骤：
1. 获取 llm_params.messages[-1].content
2. 如果 content 以 <user_message> 开头：
   - 提取 <user_message> 和 </user_message> 之间的内容
   - 去除 XML 标签，保留纯文本
3. 否则：
   - 直接使用 content 内容
4. 返回提取的用户输入文本
```

**2. 提取 AI 生成的代码（code_output）**

```
输入：单个 request 的 JSON 数据
条件：request.assistant_out_code_lines > 0
步骤：
1. 遍历 response_content.tool_calls 数组
2. 过滤 function.name 为 "write_to_file" 或 "apply_diff" 的记录
3. 对每个匹配的记录：
   - 解析 function.arguments（JSON 字符串）
   - 提取 arguments.content 字段（代码内容）
   - 提取 arguments.path 字段（文件路径，如有）
4. 返回 [{"path": "xxx", "code": "yyy"}, ...]
```

**3. 生成 Task 内容文件**

```
输入：某 task_id 下的所有 request 记录（按时间排序）
输出文件：rawdata/task/YYYY-MM/DD/$task_id.json

文件结构：
{
  "task_id": "019d4288-3775-75e0-8672-9a80f01bb988",
  "user_id": "fa50dba2-91ad-485e-92b0-483deee60d24",
  "user_name": "dengbinbox",
  "project_id": "xxx",
  "start_time": "2026-03-09T14:16:48+08:00",
  "end_time": "2026-03-09T14:20:15+08:00",
  "total_user_in_chars": 440,
  "total_code_lines": 420,
  "conversations": [
    {
      "timestamp": "2026-03-09T14:16:48+08:00",
      "request_id": "xxx",
      "user_input": "帮我写一个用户登录功能...",
      "code_outputs": [
        {"path": "/src/auth/login.go", "code": "package auth..."}
      ]
    },
    {
      "timestamp": "2026-03-09T14:18:22+08:00",
      "request_id": "yyy",
      "user_input": "再加一个密码加密...",
      "code_outputs": []
    }
  ]
}
```

**4. AI 估时分析**

```
输入：$task_id.json 文件内容
输出：{"ai_estimated_days": 4.5, "ai_estimated_reason": "理由..."} 这个内容如果生成后，可以保存到 $task_id.json中

API 配置：
- Base URL: https://open.bigmodel.cn/api/anthropic
- Model: claude-3-5-sonnet (或其他可用模型)
- Timeout: 3000000ms (5分钟)

默认提示词：
```
你是一个经验丰富的软件项目经理，擅长评估软件开发工作量。

请根据以下用户与 AI 的对话记录，分析用户的需求复杂度，并估算实现该需求所需的**人天**（一个人一天的工作量）。

重点关注：
1. 用户需求的复杂程度
2. 涉及的功能模块数量
3. 技术难度（如是否需要处理并发、安全、性能等问题）
4. 代码量规模

用户输入记录（按时间顺序）：
{{user_inputs}}

AI 生成的代码：
{{code_outputs}}

请输出：
1. 估算人天（ai_estimated_days）：纯数字，支持小数（如 0.5、2.5）
2. 估算理由（ai_estimated_reason）：简洁说明估算依据，100-200字

输出格式：JSON
{
  "ai_estimated_days": 4.5,
  "ai_estimated_reason": "该需求涉及用户认证模块开发，包含登录、注册、密码加密等功能。从对话记录看，用户通过多轮交互逐步完善需求，代码量约400行。综合考虑功能复杂度和代码规模，估算需要4.5人天。"
}
```
```

提示词配置：
- 配置文件：config.yaml → ai_estimation_prompt
- 支持自定义提示词模板
- 变量占位符：{{user_inputs}}, {{code_outputs}}, {{total_chars}}, {{total_lines}} 等

**Task 层字段来源：**

| Task 字段 | 来源 | 说明 |
|-----------|------|------|
| `task_id`, `caller`, `client_id`, `user_id`, `user_name`, `repo_id`, `project_path`, `project_id`, `client_ide`, `client_version`, `client_os`, `prompt_mode`, `mode`, `org1-org4` | 取 task 下任意一条 request 的值 | 相同 task 内这些字段相同 |
| `user_in_chars`, `assistant_out_code_lines`, `system_tokens`, `user_tokens`, `api_process_time`, `api_ttft`, `api_in_tokens`, `api_out_tokens`, `api_cost` | 对 task 下所有 request 求和 | - |
| `api_request_time` | 取 task 下所有 request 的最早时间 | - |
| `api_end_time` | 取 task 下所有 request 的最晚时间 | - |
| `api_count` | task 下 request 的数量 | - |
| `source_file` | task 内容文件路径 | `rawdata/YYYY-MM/DD/task_$task_id.json` |
| `ai_estimated_days`, `ai_estimated_reason` | AI 分析 task 内容文件生成 | 使用配置的大模型 API |

**Task 内容文件存储：**
- 路径：`rawdata/YYYY-MM/DD/task_$task_id.json`
- 用途：保留完整的用户输入和 AI 输出，用于 AI 估时分析和人工审计
- 清理策略：可按月归档，保留 1 年

### 5.3 维度聚合查询逻辑（实时从 Task 表聚合）

不再创建 Stat 物理表，维度聚合通过实时 ES Aggregation 查询实现。

**聚合查询流程：**

```
前端请求: GET /api/stat?dimension=project&startDate=20260301&endDate=20260331
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ Backend 构建 ES Aggregation 查询                         │
│ - 索引：costrict_chat_task_20260301, ... 20260331        │
│ - terms aggregation: field=project_id                    │
│ - sub-aggs:                                              │
│   - sum: user_in_chars, assistant_out_code_lines         │
│   - sum: api_in_tokens, api_out_tokens, api_cost         │
│   - sum: api_count                                       │
│   - sum: ai_estimated_days                               │
│   - value_count: task_id (task_count)                    │
│   - min: api_request_time                                │
│   - max: api_end_time                                    │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ ES 返回聚合结果                                           │
│ 每个 bucket 包含：                                        │
│ - key: project_id                                         │
│ - 各 sum/min/max 值                                       │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│ Backend 计算派生字段                                      │
│ - lead_time = max - min                                   │
│ - process_time（见下方逻辑）                              │
└────────┬─────────────────────────────────────────────────┘
         │
         ▼
返回 JSON 给前端
```

**处理时长计算逻辑（实时计算）：**

```
输入：某个维度（如 project_id=xxx）下的 Task 列表
步骤：
1. 从 ES 获取该维度下所有 task 的 api_request_time 和 api_end_time
2. 按 api_request_time 升序排序
3. 初始化：
   - segStart = 第一条.api_request_time
   - segEnd = 第一条.api_end_time
   - totalProcessTime = 0
4. 遍历每条 task（从第二条开始）：
   a. 计算 gap = 当前.api_request_time - segEnd
   b. 如果 gap ≤ 10 分钟：
      - 合并到当前段：segEnd = max(segEnd, 当前.api_end_time)
   c. 如果 gap > 10 分钟：
      - 结算当前段：totalProcessTime += segEnd - segStart
      - 开启新段：segStart = 当前.api_request_time
                  segEnd = 当前.api_end_time
5. 结算最后一段：totalProcessTime += segEnd - segStart
6. 返回 totalProcessTime（毫秒）作为 process_time
```

**各维度聚合键：**

| 维度 | ES Aggregation Field | 说明 |
|------|---------------------|------|
| project | `project_id` | 按项目聚合 |
| repo | `repo_id` | 按仓库聚合 |
| user | `user_id` | 按用户聚合 |
| org1 | `org1` | 按公司聚合 |
| org2 | `org1_org2`（需添加 scripted field 或 keyword） | 按体系聚合 |
| org3 | `org1_org2_org3` | 按部门聚合 |
| org4 | `org1_org2_org3_org4` | 按团队聚合 |

**汇总命令：**

```bash
# 仅生成持久化层（request + task）
kbcli reindex --date 20260331

# 单独执行某一步
kbcli reindex --date 20260331 --step=request   # 仅生成 request 层
kbcli reindex --date 20260331 --step=task      # 仅生成 task 层

# 强制覆盖
kbcli reindex --date 20260331 --force
```

---

## 六、提效分析（核心功能）

提效分析是本系统的核心功能，通过对比 AI 预估人天与实际编码耗时，计算提效比例。

### 6.1 分析维度

支持两个分析维度：

| 维度 | 标识 | 说明 | 特殊逻辑 |
|------|------|------|----------|
| Project | `project_id` | 按项目统计 | 基础提效计算 |
| Repo | `repo_id` | 按仓库统计 | 额外分析 git commit 记录 |

### 6.2 核心指标定义

#### 6.2.1 AI 预估指标（来自 Task 表）

| 指标 | 字段 | 计算方式 |
|------|------|----------|
| AI 预估总人天 | `ai_estimated_days` | SUM(task.ai_estimated_days) |
| AI 预估总成本 | `ai_estimated_cost` | SUM(task.api_cost) |
| 生成代码行数 | `total_code_lines` | SUM(task.assistant_out_code_lines) |
| 任务数量 | `task_count` | COUNT(task.task_id) |

#### 6.2.2 实际耗时指标（按用户归并计算）

对于每个用户，计算其在该维度下的实际参与情况：

```
用户实际耗时计算：
- user_start_time = MIN(task.api_request_time)  // 该用户最早开始时间
- user_end_time = MAX(task.api_end_time)        // 该用户最晚结束时间
- user_lead_time = user_end_time - user_start_time  // 用户总耗时
- user_process_time = SUM(task.api_process_time)    // 用户实际处理时长
```

汇总所有用户：

| 指标 | 字段 | 计算方式 |
|------|------|----------|
| 总 Lead Time | `total_lead_time` | SUM(user_lead_time) |
| 总 Process Time | `total_process_time` | SUM(user_process_time) |
| 参与用户数 | `user_count` | COUNT(DISTINCT task.user_id) |
| 最早开始时间 | `start_time` | MIN(task.api_request_time) |
| 最晚结束时间 | `end_time` | MAX(task.api_end_time) |

#### 6.2.3 提效比例计算

```
提效比例(Lead Time) = ai_estimated_days / (total_lead_time / 8小时) × 100%
提效比例(Process Time) = ai_estimated_days / (total_process_time / 8小时) × 100%

说明：
- lead_time 和 process_time 单位是毫秒，需转换为天数（除以 8小时 = 28800000ms）
- 比值 > 100% 表示提效，< 100% 表示耗时更多
```

#### 6.2.4 成本相关指标

| 指标 | 字段 | 说明 |
|------|------|------|
| AI API 总成本 | `total_api_cost` | SUM(task.api_cost) |
| 预估人工成本 | `estimated_human_cost` | ai_estimated_days × 人天单价 |
| 成本节省 | `cost_saving` | estimated_human_cost - total_api_cost |
| ROI | `roi` | cost_saving / total_api_cost × 100% |

### 6.3 Repo 维度特殊分析

Repo 维度除了基础提效计算外，还需要分析 git commit 记录：

#### 6.3.1 Git Commit 关联分析

**核心目标**：建立 Git Commit 与 Task 的关联，识别代码来源（AI 生成/人工/其他 AI）。

**输入数据**：
- Task 表数据（user_id, task 内容, 时间）
- Git Log（commit 作者、时间、代码变更）
- 用户映射表（org_csv_file 中的 git_user_id/name）

**关联逻辑**：

| 关联类型 | 判断条件 |
|---------|---------|
| **强关联** | commit 作者匹配 git_user_id，且 commit 时间接近 task 时间（±10分钟），代码变更匹配 task 输出 |
| **弱关联** | commit 作者匹配但时间有偏差，或时间匹配但作者需通过时间窗口推断 |
| **无关联** | 无法匹配到 task |

**代码来源分类**：

| 来源 | 判断标准 |
|------|---------|
| **ai_current** | 强/弱关联到 task，代码与 task 输出匹配 |
| **human** | commit 作者在项目成员中，但无关联 task |
| **ai_other** | commit message 含 "AI/Copilot" 关键词，但无关联 task |
| **unknown** | 无法确定来源 |

**输出**：
- **文件**：`code_source_{repo_id}_{date}.json` - 存每个 commit 的详细关联信息
- **数据库**：`repo_metrics` 表 - 存代码来源分类统计（commit 数、代码行数、预估人天）

**AI 预估**：
- AI 生成代码：使用 task.ai_estimated_days 或重新分析 task 内容
- 人工/其他代码：调用 AI API 分析 commit 内容估算人天



#### 6.3.2 Repo 预估人天二次分析

基于代码来源分类数据，进行精细化人天预估：

```
输入：
- AI 生成代码统计（已关联的 task 数据）
- 人工代码统计（需要 AI 分析的 commit）
- 其他 AI 代码统计

AI 分析流程：

1. AI 生成代码人天（当前系统）
   - 直接使用 task.ai_estimated_days 累加
   - 或使用 AI 重新分析 task 对话内容
   - 这部分是"我们的 AI"提效的基准

2. 人工代码人天预估
   对每个未关联 task 的 commit，调用 AI API 分析代码变更，估算人工开发人天

3. 综合预估与提效计算
   - repo_ai_estimated_days = ai_current_days + human_days + ai_other_days
   - AI 提效（当前系统）= ai_current_days / actual_process_time
   - 整体提效 = repo_ai_estimated_days / actual_process_time
```

**提示词配置**：AI 分析提示词可通过 `config.yaml` 自定义，支持变量占位符（commit_message, code_diff, lines_added 等）

### 6.4 数据存储边界

明确区分**数据库存储**和**文件存储**的边界：

| 存储类型 | 适用场景 | 具体内容 |
|---------|---------|---------|
| **PostgreSQL** | 结构化查询、聚合统计、需纠错的元数据 | 项目/仓库维度汇总指标、用户参与记录、代码来源统计、提效比例、纠错记录 |
| **JSON 文件** | 详细审计、大体积原始数据、计算过程溯源 | Task 对话内容、Git Commit 详情、Commit-Task 关联映射、代码行级分析 |

#### 6.4.1 文件目录结构

```
rawdata/
├── YYYY-MM/
│   ├── DD/
│   │   ├── task_{task_id}.json          # Task 内容文件（存 conversations）
│   │   └── request_*.json               # 原始请求文件
│   └── analysis/                        # 分析结果文件
│       ├── project_{project_id}_{YYYYMMDD}.json      # Project 提效分析（完整计算过程）
│       ├── repo_{repo_id}_{YYYYMMDD}.json            # Repo 提效分析
│       └── code_source_{repo_id}_{YYYYMMDD}.json     # 代码来源详细分析（含 commit-task 映射）
```

**文件 vs 数据库存储说明：**

- **Task 内容文件**（`task_{task_id}.json`）：存 conversations 数组（用户输入+AI 输出），用于 AI 分析和审计
- **分析文件**（`project/repo_*.json`）：存完整计算过程，用于回溯和人工审查
- **代码来源文件**（`code_source_*.json`）：存每个 commit 的详细关联信息，数据量大，不适合存数据库
- **数据库**：存汇总后的统计值，用于查询和展示

### 6.5 PostgreSQL 元信息表设计

存储汇总后的分析结果，支持查询、纠错和自定义计算。

**核心表说明：**

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `project_metrics` | Project 维度汇总 | 预估人天(raw/corrected)、实际耗时、提效比例、成本、溯源文件路径 |
| `repo_metrics` | Repo 维度汇总 | 同上 + 代码来源分类统计(ai_current/human/ai_other) |
| `git_task_mappings` | Commit-Task 关联（可选）| commit_hash、task_id、关联类型、代码来源 |
| `correction_history` | 纠错审计 | 修改字段、旧值、新值、原因、操作人 |

**设计原则：**
- 原始 AI 预估值（`raw_*`）不可修改，用于审计
- 用户纠正值（`corrected_*`）可修改，UI 展示时优先使用
- 详细数据（如每个 commit 的关联详情）存 JSON 文件，不存数据库
- 提效比例根据 corrected 值自动重算

**纠错流程：**
1. 系统计算 → 写入 `raw_ai_estimated_days`
2. 用户纠错 → 写入 `corrected_ai_estimated_days` + `correction_reason`
3. 触发重算 → 更新 `efficiency_ratio_*`
4. 记录审计 → 写入 `correction_history`

### 6.6 CLI 分析命令

### 6.6 CLI 分析命令

```bash
# 分析指定 project
kbcli analyze --dimension=project --id="xxx" --start-date=20260301 --end-date=20260331

# 分析指定 repo（包含 git 分析）
kbcli analyze --dimension=repo --id="https://github.com/xxx/yyy.git" --start-date=20260301 --end-date=20260331

# 重新分析（覆盖已有结果）
kbcli analyze --dimension=project --id="xxx" --force

# 批量分析所有 project
kbcli analyze --dimension=project --all --start-date=20260301 --end-date=20260331

# 纠错（更新 PostgreSQL 中的纠正值）
kbcli correct --dimension=project --id="xxx" --field=ai_estimated_days --value=50.5 --reason="功能复杂度被低估" --by="admin"

# 自定义提效计算公式
kbcli set-formula --dimension=repo --id="xxx" \
  --formula="(ai_current_days + human_days * 0.5) / process_time * 100" \
  --notes="人工代码只算50%工作量"

# 导出分析报告
kbcli export --dimension=project --id="xxx" --format=pdf --output=./report.pdf

# ============================================
# Git 代码来源分析命令（核心）
# ============================================

# 分析代码来源（自动关联 Git Commit 与 Task）
kbcli analyze-code-source --dimension=repo --id="xxx" \
  --start-date=20260301 --end-date=20260331

# 重新分析代码来源（覆盖已有结果）
kbcli analyze-code-source --dimension=repo --id="xxx" --force

# 查看 Git-Task 关联详情
kbcli show-mappings --dimension=repo --id="xxx" --date=20260331

# 手动校准代码来源
kbcli calibrate-code-source --commit="abc123" \
  --code-source="ai_current" \
  --task-id="task-xxx" \
  --reason="代码与 Task 输出匹配"

# 查看代码来源统计
kbcli code-source-stats --dimension=repo --id="xxx" --date=20260331

# 导出代码来源分析结果
kbcli export-code-source --dimension=repo --id="xxx" --format=json --output=./code-source.json
```

---

## 七、数据展示设计

### 7.1 看板页面（Dashboard）

参考 `comdigger` 的 dashboard 设计，提供两个视图：

#### 7.1.1 Request 数据视图
- **筛选条件**：日期范围（默认最近 7 天）
- **展示字段**：
  | 字段 | 说明 |
  |------|------|
  | @timestamp | 时间 |
  | user_name | 用户名 |
  | project_id | 项目 ID |
  | model | 模型 |
  | sender | 发送者 |
  | user_in_chars | 输入字符数 |
  | assistant_out_code_lines | 代码行数 |
  | api_process_time | 处理时长 |
  | api_ttft | 首 token 时延 |
  | api_in_tokens | 输入 tokens |
  | api_out_tokens | 输出 tokens |
  | api_cost | 费用 |

#### 7.1.2 聚合数据视图（实时从 Task 表查询）
- **维度切换**：项目 / 用户 / 组织（org1-org4）
- **数据来源**：实时从 Task 表 ES Aggregation 查询
- **展示字段**：
  | 字段 | 说明 |
  |------|------|
  | union_id | 项目/用户/组织 ID |
  | user_in_chars | 输入字符数（sum） |
  | assistant_out_code_lines | 代码行数（sum） |
  | api_count | API 次数（sum） |
  | api_cost | API 费用（sum） |
  | api_in_tokens | 输入 tokens（sum） |
  | api_out_tokens | 输出 tokens（sum） |
  | task_count | 任务数量（value_count） |
  | ai_estimated_days | AI 估算总人天（sum） |
  | start_time | 开始时间（min） |
  | end_time | 结束时间（max） |
  | lead_time | 前置时长（计算） |
  | process_time | 处理时长（计算） |

### 7.2 提效分析面板（核心功能）

提效分析面板是本系统的核心功能，展示 AI 辅助编程的提效比例。

#### 7.2.1 提效概览页

**维度切换**：Project / Repo

**核心指标卡片区**：

| 指标卡 | 展示内容 |
|--------|----------|
| AI 预估人天 | `ai_estimated_days` 天，可点击纠错 |
| 实际 Lead Time | `total_lead_time` 转换为天数 |
| 实际 Process Time | `total_process_time` 转换为天数 |
| 提效比例(Lead) | `efficiency_ratio_lead`%，带颜色标识（>100%绿色，<100%红色） |
| 提效比例(Process) | `efficiency_ratio_process`%，带颜色标识 |
| 投入成本 | `api_cost` 元 |
| 节省成本 | `cost_saving` 元 |
| ROI | `roi`% |

**用户参与详情表**：

| 用户 | 开始时间 | 结束时间 | Lead Time | Process Time | 占比 |
|------|----------|----------|-----------|--------------|------|
| 张三 | 2026-03-01 | 2026-03-20 | 19天 | 6.7天 | 67% |
| 李四 | 2026-03-05 | 2026-03-25 | 20天 | 3.3天 | 33% |
| **合计** | - | - | **24.5天** | **10天** | **100%** |

#### 7.2.2 提效详情页（下钻）

**页面结构**：

1. **时间线视图**
   - 展示项目生命周期时间轴
   - 标记每个用户的参与时段
   - 展示 task 分布热力图

2. **指标对比图表**
   - 柱状图：AI 预估人天 vs 实际 Lead Time vs 实际 Process Time
   - 饼图：成本构成（API 成本 vs 预估人工成本）
   - 趋势图：每日代码产出趋势

3. **代码来源分析区（核心）**
   - **代码来源饼图**：展示 AI 生成/人工/其他 AI/未知 的代码占比
   - **代码来源统计表**：
     | 来源 | Commit 数 | 代码行数 | 预估人天 | 占比 |
     |------|-----------|----------|----------|------|
     | AI 生成（当前系统） | 45 | 3,200 | 35.5天 | 77% |
     | 人工编写 | 12 | 800 | 8.0天 | 17% |
     | 其他 AI 工具 | 5 | 300 | 2.5天 | 5% |
     | 未知来源 | 6 | 150 | - | 1% |
   - **关联详情表**：
     | Commit | 作者 | 时间 | Task ID | 关联类型 | 代码来源 | 操作 |
     |--------|------|------|---------|----------|----------|------|
     | abc123 | dengbin | 03-15 | task-001 | 强关联 | AI 生成 | 校准 |
     | def456 | zhangsan | 03-16 | - | 无关联 | 人工 | 关联 |
   - **Git 提交热力图**：按时间展示 Commit 分布

4. **AI 提效计算区**
   - **基础提效**：所有代码 / 实际耗时
   - **AI 提效**（当前系统）：AI 代码 / 实际耗时（核心指标）
   - **综合提效**：加权计算（支持自定义公式）
   - **自定义公式编辑器**：
     - 提供变量选择器（ai_current_days, human_days, process_time 等）
     - 公式预览和验证
     - 保存自定义公式

5. **计算过程溯源**
   - 提供"查看计算过程"按钮
   - 弹出 JSON 展示原始分析文件内容
   - 支持下载分析文件
   - 支持下载代码来源分析文件

#### 7.2.3 纠错功能

**纠错入口**：
- 在 AI 预估人天指标卡上显示"编辑"图标
- 点击弹出纠错对话框

**纠错表单**：
- 原始值：只读显示 `raw_ai_estimated_days`
- 纠正值：输入框，默认等于原始值
- 纠正原因：必填，文本域
- 操作人：自动填充当前登录用户

**纠正后展示**：
- 指标卡显示纠正值
- 标注"已人工校准"标签
- 鼠标悬停显示原始值和纠正原因
- 显示"查看纠错历史"链接

### 7.3 项目面板（Project Panel）

**入口**：用户选择 project_id 后下钻

**页面 1：项目列表**
- 数据来源：project_metrics 表（PG）或实时计算
- 展示所有项目的提效概览
- 关键指标：提效比例、AI 预估人天、实际耗时、成本节省
- 支持搜索和排序
- 点击跳转提效分析详情

**页面 2：提效分析详情**
- 复用 7.2 提效分析面板
- 额外展示任务列表
- 可下钻到单个 task

### 7.4 仓库面板（Repo Panel）

类似项目面板，但额外展示 Git 分析数据。

### 7.5 用户面板（User Panel）

- 展示用户的 AI 编码活动
- 项目参与情况
- 时间分布热力图
- **新增：个人提效统计**
  - 参与的 Project/Repo 列表
  - 个人贡献的 AI 预估人天
  - 个人实际投入时间

### 7.6 组织面板（Org Panel）

**层级导航**：
- org1（公司）→ org2（体系）→ org3（部门）→ org4（团队）
- 支持逐级下钻

**展示内容**：
- **新增：组织提效对比**
  - 各单元提效比例排名
  - AI 采用率统计
  - 成本节省汇总

---

## 八、API 接口设计

### 8.1 索引管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/indices` | 获取所有索引列表（request/task） |

### 8.2 Request 数据查询

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/requests` | startDate, endDate, page, pageSize, taskId | 查询原始请求记录 |

### 8.3 Task 数据查询

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/tasks` | startDate, endDate, taskId, projectId, userId | 查询任务列表 |
| GET | `/api/tasks/summary` | startDate, endDate | 获取任务汇总统计 |

### 8.4 维度聚合查询（实时从 Task 表聚合）

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/aggregate` | startDate, endDate, dimension, page, pageSize | 实时聚合查询（从 Task 层聚合） |
| GET | `/api/aggregate/summary` | startDate, endDate, dimension | 获取指定维度汇总（从 Task 层聚合） |

**维度参数说明：**
- `dimension`: project, repo, user, org1, org2, org3, org4
- 实时使用 ES Aggregation 从 Task 表查询

### 8.5 提效分析接口

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/analysis/efficiency` | dimension, id, startDate, endDate | 获取提效分析数据 |
| POST | `/api/analysis/efficiency/calculate` | dimension, id, startDate, endDate | 触发重新计算 |
| GET | `/api/analysis/efficiency/file` | dimension, id, date | 下载分析过程文件 |
| PUT | `/api/analysis/efficiency/correct` | dimension, id, field, value, reason | 纠正预估人天 |
| GET | `/api/analysis/efficiency/history` | dimension, id | 获取纠错历史 |

**响应示例：**
```json
{
  "dimension": "project",
  "dimension_id": "xxx",
  "analysis_date": "2026-03-31",
  
  "ai_estimated": {
    "raw_days": 45.5,
    "corrected_days": 50.0,
    "is_corrected": true,
    "correction_reason": "功能复杂度被低估"
  },
  
  "actual_time": {
    "total_lead_time_ms": 2116800000,
    "total_process_time_ms": 864000000,
    "user_count": 3,
    "users": [...]
  },
  
  "efficiency": {
    "ratio_lead": 185.7,
    "ratio_process": 455.0,
    "ratio_lead_display": "185.7%",
    "ratio_process_display": "455.0%"
  },
  
  "cost": {
    "api_cost": 156.8,
    "estimated_human_cost": 18200.0,
    "daily_rate": 400.0,
    "saving": 18043.2,
    "roi": 11405.0
  },
  
  "analysis_file": "/rawdata/2026-03/analysis/project_xxx_20260331.json"
}
```

### 8.6 Git 分析接口（Repo 维度）

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/analysis/git` | repo_id, startDate, endDate | 获取 Git 分析数据 |
| POST | `/api/analysis/git/analyze` | repo_id, startDate, endDate | 触发 Git 分析 |
| GET | `/api/analysis/git/commits` | repo_id, startDate, endDate | 获取 Commit 列表 |

### 8.7 代码来源分析接口（核心）

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/code-source` | dimension, id, startDate, endDate | 获取代码来源统计 |
| POST | `/api/code-source/analyze` | dimension, id, startDate, endDate | 触发代码来源分析 |
| GET | `/api/code-source/mappings` | dimension, id, date | 获取 Commit-Task 关联列表 |
| PUT | `/api/code-source/calibrate` | commit_hash, code_source, task_id, reason | 校准代码来源 |
| GET | `/api/code-source/file` | dimension, id, date | 下载代码来源分析文件 |
| PUT | `/api/code-source/formula` | dimension, id, formula, notes | 设置自定义提效公式 |

**代码来源统计响应示例：**
```json
{
  "dimension": "repo",
  "dimension_id": "https://github.com/xxx/yyy.git",
  "analysis_date": "2026-03-31",
  
  "code_source": {
    "ai_current": {
      "commits": 45,
      "lines": 3200,
      "estimated_days": 35.5,
      "percentage": 77,
      "associated_tasks": ["task-001", "task-002"]
    },
    "human": {
      "commits": 12,
      "lines": 800,
      "estimated_days": 8.0,
      "percentage": 17
    },
    "ai_other": {
      "commits": 5,
      "lines": 300,
      "estimated_days": 2.5,
      "percentage": 5
    },
    "unknown": {
      "commits": 6,
      "lines": 150,
      "percentage": 1
    }
  },
  
  "efficiency": {
    "ai_current_only": {
      "ratio": 417.6,
      "calculation": "35.5 / 8.5 * 100%"
    },
    "overall": {
      "ratio": 460.0,
      "calculation": "46.0 / 10.0 * 100%"
    },
    "custom": {
      "ratio": 520.0,
      "formula": "(ai_current_days + human_days * 0.5) / process_time * 100"
    }
  },
  
  "analysis_file": "/rawdata/2026-03/analysis/code_source_repo_xxx_20260331.json"
}
```

---

## 九、已知问题与 TODO

### 8.1 当前实现问题

1. **ES Handler 中的维度聚合查询**：需实现从 Task 表实时聚合查询（ES Aggregation），不再查询 Stat 表

### 8.2 待实现功能

**数据层：**
1. **Task 聚合与 AI 估时**：实现 task 数据提取和 AI 分析流程
2. **历史数据按月归并**：2 月以前数据按月汇总，删除日索引

**提效分析（核心）：**
3. **提效计算引擎**：
   - 实现 project/repo 维度提效计算
   - 按用户归并计算 lead_time / process_time
   - 生成分析过程文件
4. **Git 分析模块**：
   - 集成 git 命令获取 commit 记录
   - 基于 commit 进行二次 AI 预估
5. **代码来源分析（核心）**：
   - 实现 Git Commit 与 Task 关联算法
   - 代码来源分类（AI 当前系统/人工/其他 AI/未知）
   - 提交者身份映射（org_csv_file git_user 字段）
   - 代码行数统计和 AI 预估
6. **PostgreSQL 元信息表**：
   - 创建 project_metrics / repo_metrics 表
   - 创建 git_task_mappings 关联表
   - 创建 code_source_summary 汇总表
   - 实现纠错功能（corrected_ai_estimated_days）
   - 实现自定义提效公式功能
   - 创建 correction_history 审计表
6. **提效分析 API**：
   - `/api/analysis/efficiency` 查询接口
   - `/api/analysis/efficiency/correct` 纠错接口
   - Git 分析相关接口

**可视化层：**
7. **看板图表可视化**：添加趋势图、饼图等可视化组件
8. **提效分析面板**：
   - 提效概览页（指标卡片、用户参与表）
   - 提效详情页（时间线、对比图表）
   - 纠错功能 UI
9. **权限控制**：添加用户登录和权限管理

### 8.3 优化项

1. **批量写入优化**：ES 批量写入支持重试和并发控制
2. **数据校验**：原始数据解析增加更多字段校验
3. **错误处理**：完善错误日志和监控
4. **缓存层**：热点数据添加 Redis 缓存

---

## 十、附录

### 9.1 ES 常用查询

```bash
# 查看所有索引
GET _cat/indices?v

# 查询 request 索引（原始请求）
GET costrict_chat_request_20260331/_search
{
  "query": { "match_all": {} },
  "size": 100
}

# 查询 task 索引（任务聚合）
GET costrict_chat_task_20260331/_search
{
  "query": { "match_all": {} },
  "size": 100
}

# 按项目实时聚合查询（替代 stat 表）
GET costrict_chat_task_20260331/_search
{
  "size": 0,
  "aggs": {
    "by_project": {
      "terms": { "field": "project_id", "size": 100 },
      "aggs": {
        "task_count": { "value_count": { "field": "task_id" } },
        "user_in_chars": { "sum": { "field": "user_in_chars" } },
        "code_lines": { "sum": { "field": "assistant_out_code_lines" } },
        "api_count": { "sum": { "field": "api_count" } },
        "api_cost": { "sum": { "field": "api_cost" } },
        "api_in_tokens": { "sum": { "field": "api_in_tokens" } },
        "api_out_tokens": { "sum": { "field": "api_out_tokens" } },
        "ai_estimated_days": { "sum": { "field": "ai_estimated_days" } },
        "start_time": { "min": { "field": "api_request_time" } },
        "end_time": { "max": { "field": "api_end_time" } }
      }
    }
  }
}

# 按用户实时聚合查询
GET costrict_chat_task_20260331/_search
{
  "size": 0,
  "aggs": {
    "by_user": {
      "terms": { "field": "user_id", "size": 100 },
      "aggs": {
        "task_count": { "value_count": { "field": "task_id" } },
        "user_in_chars": { "sum": { "field": "user_in_chars" } },
        "api_cost": { "sum": { "field": "api_cost" } },
        "ai_estimated_days": { "sum": { "field": "ai_estimated_days" } }
      }
    }
  }
}
```

### 9.2 配置示例（config.yaml）

```yaml
elasticsearch:
  url: "http://localhost:9200"
  username: ""
  password: ""

rawdata_dir: "../rawdata"

# AI 估时 API 配置
ai_estimation:
  enabled: true
  api_key: "c0bebb770966452d9c415863d59c5161.alDVcnduh2Dgy3dN"
  base_url: "https://open.bigmodel.cn/api/anthropic"
  model: "claude-3-5-sonnet-20241022"
  timeout_ms: 3000000
  # 自定义提示词（可选，不配置则使用默认提示词）
  prompt: |
    你是一个经验丰富的软件项目经理，擅长评估软件开发工作量。
    
    请根据以下用户与 AI 的对话记录，分析用户的需求复杂度，并估算实现该需求所需的**人天**。
    
    重点关注：
    1. 用户需求的复杂程度
    2. 涉及的功能模块数量
    3. 技术难度（如是否需要处理并发、安全、性能等问题）
    4. 代码量规模
    
    用户输入记录：
    {{user_inputs}}
    
    AI 生成的代码：
    {{code_outputs}}
    
    总输入字符数：{{total_chars}}
    总代码行数：{{total_lines}}
    
    请输出 JSON 格式：
    {
      "ai_estimated_days": 4.5,
      "ai_estimated_reason": "估算理由..."
    }

model_prices:
  "GLM-4.7":
    in_price: 0.5
    out_price: 0.5
  "claude-3-5-sonnet":
    in_price: 3.0
    out_price: 15.0

# 组织信息 CSV 文件路径
# CSV 格式：user_id, user_name, org1, org2, org3, org4, git_user_id, git_user_name
# 匹配优先级：user_id > user_name
# Git 用户用于关联 Git Commit
org_csv_file: "./org_mapping.csv"
```

**CSV 文件格式示例**（`org_mapping.csv`）：

```csv
user_id,user_name,org1,org2,org3,org4,git_user_id,git_user_name
fa50dba2-91ad-485e-92b0-483deee60d24,dengbinbox,示例公司,研发体系,平台部,基础设施组,dengbin,dengbinbox
a1b2c3d4-e5f6-7890-abcd-ef1234567890,zhangsan,示例公司,研发体系,后端组,支付小组,zhangsan,zhangsan
,wangwu,示例公司,产品体系,产品一部,用户产品组,wangwu,wangwu1990
```

**字段说明：**
| 字段 | 必填 | 说明 |
|------|------|------|
| `user_id` | 否 | 系统用户 UUID，用于 request/task 表关联 |
| `user_name` | 否 | 系统用户名，user_id 为空时使用 |
| `org1-org4` | 否 | 组织架构信息 |
| `git_user_id` | 否 | Git 用户 ID（如 GitHub ID），用于关联 Git Commit |
| `git_user_name` | 否 | Git 提交用户名（git log 中的 Author） |

**匹配规则**：
1. 第一行是表头，必须包含 user_id, user_name
2. user_id 和 user_name 至少填写一个
3. 优先按 user_id 匹配 request/task 数据
4. 如果未匹配到，按 user_name 匹配
5. git_user_id/git_user_name 用于 Git Commit 关联（可选）
6. 支持热更新：替换 CSV 文件后执行 `kbcli reload-org` 或重启服务

### 9.3 CLI 命令

```bash
# 完整流程：request → task
kbcli reindex --date 20260331

# 单独执行某一步
kbcli reindex --date 20260331 --step=request   # 仅生成 request 层
kbcli reindex --date 20260331 --step=task      # 仅生成 task 层（依赖 request）

# 强制覆盖（删除已有索引后重新生成）
kbcli reindex --date 20260331 --force

# 指定日期范围
kbcli reindex --start-date 20260301 --end-date 20260331

# 重新加载组织信息 CSV（热更新）
kbcli reload-org

# 验证组织信息配置
kbcli validate-org --csv-file ./org_mapping.csv

# ============================================
# 提效分析命令（核心功能）
# ============================================

# 分析指定 project 的提效数据
kbcli analyze --dimension=project --id="xxx" --start-date=20260301 --end-date=20260331

# 分析指定 repo 的提效数据（包含 git 分析）
kbcli analyze --dimension=repo --id="https://github.com/xxx/yyy.git" --start-date=20260301 --end-date=20260331

# 重新分析（覆盖已有结果）
kbcli analyze --dimension=project --id="xxx" --force

# 批量分析所有 project
kbcli analyze --dimension=project --all --start-date=20260301 --end-date=20260331

# 仅执行 Git 分析（Repo 维度）
kbcli analyze git --repo-id="xxx" --start-date=20260301 --end-date=20260331

# 纠错（更新 PostgreSQL 中的纠正值）
kbcli correct --dimension=project --id="xxx" \
  --field=ai_estimated_days --value=50.5 \
  --reason="功能复杂度被低估" --by="admin"

# 导出分析报告
kbcli export --dimension=project --id="xxx" --format=pdf --output=./report.pdf

# 查看分析过程文件
kbcli cat-analysis --dimension=project --id="xxx" --date=20260331
```

### 9.4 Task 内容文件格式示例

Task 内容文件存储了完整的用户输入和 AI 输出，用于 AI 估时分析和人工审计。

**文件路径**：`task/YYYY-MM/DD/$task_id.json`

```json
{
  "task_id": "019d4288-3775-75e0-8672-9a80f01bb988",
  "user_id": "fa50dba2-91ad-485e-92b0-483deee60d24",
  "user_name": "dengbinbox",
  "project_id": "7fc2b9a3bd:d:\\project\\costrict-all\\user-indicator-collection",
  "start_time": "2026-03-09T14:16:48+08:00",
  "end_time": "2026-03-09T14:20:15+08:00",
  "total_user_in_chars": 440,
  "total_code_lines": 420,
  //TODO 补充AI分析结果
  "conversations": [
    {
      "timestamp": "2026-03-09T14:16:48+08:00",
      "request_id": "019d4288-88b6-735b-ab94-3dd61b39dc48",
      "user_input": "帮我写一个用户登录功能，需要支持邮箱和密码验证，密码需要加密存储。",
      "code_outputs": [
        {
          "path": "/src/auth/login.go",
          "code": "package auth\n\nimport (\n    \"crypto/bcrypt\"\n    \"database/sql\"\n)\n\nfunc Login(email, password string) (*User, error) {\n    // 查询用户...\n}"
        }
      ]
    },
    {
      "timestamp": "2026-03-09T14:18:22+08:00",
      "request_id": "019d4288-3775-75e0-8672-9a80f01bb989",
      "user_input": "再加一个密码强度校验，要求至少8位，包含大小写字母和数字。",
      "code_outputs": [
        {
          "path": "/src/auth/validator.go",
          "code": "package auth\n\nimport \"regexp\"\n\nfunc ValidatePassword(password string) bool {\n    // 密码强度校验逻辑...\n}"
        }
      ]
    },
    {
      "timestamp": "2026-03-09T14:20:15+08:00",
      "request_id": "019d4288-3775-75e0-8672-9a80f01bb990",
      "user_input": "好的，就这样",
      "code_outputs": []
    }
  ]
}
```

**字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `task_id` | string | 任务唯一标识 |
| `user_id` | string | 用户 UUID |
| `user_name` | string | 用户名 |
| `project_id` | string | 项目 ID |
| `start_time` | string | 任务开始时间（第一条 request） |
| `end_time` | string | 任务结束时间（最后一条 request） |
| `total_user_in_chars` | int | 用户输入总字符数 |
| `total_code_lines` | int | AI 生成代码总行数 |
| `conversations` | array | 对话记录列表 |
| `conversations[].timestamp` | string | 该轮对话时间 |
| `conversations[].request_id` | string | 请求 ID |
| `conversations[].user_input` | string | 用户输入内容（从 `<user_message>` 提取） |
| `conversations[].code_outputs` | array | AI 生成的代码列表 |
| `conversations[].code_outputs[].path` | string | 代码文件路径 |
| `conversations[].code_outputs[].code` | string | 代码内容 |

# task数据

## 客户端生成 task_conversation.jsonl
以用户能看到的视角，对每次request对话做append方式记录，建议客户端存储路径为：task/conversation/2026/04/01/$task_id.json 这个2026/04/01是指task start time（最好是服务器端返回的时间，保持时间一致性）的年月日。
如下是jsonl的**一行request**数据，最终每次request就是一行，这样方便程序append（注意，一定是按时间顺序排序的，时间一定是服务器时间为主），这个数据不要存储到数据库，占空间。
```
{
  "sender": "user", # 这次对话是谁发起的，大概是user，还是agent 
  "request_id": "019d4288-88b6-735b-ab94-3dd61b39dc48",
  "prompt_mode": "vibe",
  "mode": "code",
  "model": "GLM-4.7",
  "start_time": "2026-03-29T10:07:04Z", # 请求开始时间
  "end_time": "2026-03-29T10:11:57Z", # 请求结束时间
  "process_time": 8507, # end_time - start_time 得到的毫秒数
  "process_ttft": 8422, # 首token响应的延时毫秒数，这些记录在原始数据里面已经存在
  
  "upstream_tokens": 434 # 输入给AI的tokens数量
  "downstream_tokens": 4342 # AI输出的tokens数量
  
  "cost": 1.43 # 单位元，upstream_tokens和downstream_tokens model结合计算此次请求的费用
  
  "request_content": "不管是用户输入的，还是agent输入的，都放到这里", # 注意这里是不带历史对话记录的，我希望串行累加 request_content 能得到用户视图看到的对话记录，而且这样会比较小
  "response_content": "AI API返回的内容",
  
  "user_input": "如果sender 是user，则提取request_content中用户输入内容到这里"
  "diff": ""  #  如果response_content是要调用工具apply_diff或Writefile写代码文件，则应该把写入文件的内容放到这里
  "diff_lines": 1494 # diff中新增和修改的代码行数
  "error_code": 231 #如果有错误信息，则记录错误
  "error_reason": "如果有错误，则这里是错误描述"
}
```

## 客户端生成 task_summary.json
这是task的汇总summary，可以基于task_conversation.jsonl来生成, 建议客户端存储路径为：task/summary/2026/04/01/$task_id.json 这个2026/04/01是指task start time（最好是服务器端返回的时间，保持时间一致性）的年月日。

客户端上传上来的 task/summary/2026/04/01/$task_id.json 字段示例：
```
{
  "task_id": "019d3914-eee8-74df-83c6-481b72521f89",
  
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920", # 冗余字段，方便排障
  
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "client_ide": "vscode", # 以前的字段，应该是vscode,  jetbrains系列， cli
  "client_version": "2.5.3",
  "client_os": "Windows",
  "client_os_version": "7.1.323",
  
  "caller": "chat", # chat 或者 codereview
  
  "repo_addr": "https://github.com/zgsm-ai/costrict.git", # 地址
  "repo_branch":"main",  # 分支名称
  "work_dir": "d:\\project\\costrict-all\\user-indicator-collection", # 原来叫project_path，修改名字为work_dir
  
  "diff": ""  # task开始和结束时的diff
  "diff_lines": 1494 # diff中新增和修改的代码行数
}
```

task/conversation/2026/04/01/$task_id.json 和 task/summary/2026/04/01/$task_id.json 数据都要同步到服务器上。

## 服务端写入tasks表

服务端的数据库叫costrict_stat，密码1，后续所有的表都存储到这个数据下

服务端分析程序会扫描task/summary/目录下的所有文件，
预估时间：
  
直接存储到es数据库， 包含task/summary/2026/04/01/$task_id.json 内容（排除diff字段），再新增如下字段，表名tasks
```
{
  "work_dir_id": "7fc2b9-d-project-costrict-all-user-indicator-collection", # client_id前6位 + "-" + project_path, 只保留数字和字母，其他字符用-替代，多个-合并为一个，移除首尾-
  
  "start_time": "2026-03-29T10:07:04Z", # task_conversation.jsonl 中第一条的start_time
  "end_time": "2026-03-29T10:11:57Z", # task_conversation.jsonl 中最后一条的end_time

  "upstream_tokens": 434, # 输入给AI的tokens数量，task_conversation.jsonl 中的对应字段累加
  "downstream_tokens": 4342, # AI输出的tokens数量，同上
  "cost": 1.43, # 单位元，同上
  "title": "这是一个什么任务" # 通过AI提取一下task的描述信息，比如这个task是做什么的，长度不超过 100个字符。

  "task_real_minutes": 12.5, # 任务实际耗时(分钟)，基于对话时间序列计算
  "task_real_minutes_reason": "片段1: 10:20:59~10:29:59 (4.0分钟+5.0分钟延长) | 片段2: 12:34:59~12:40:59 (1.0分钟+5.0分钟延长) | 合计=15.0分钟",
  "task_real_minutes_manual": null, # 人工校准值，如果用户认为自动计算不准确可以手动修正
  "task_real_minutes_reason_manual": null, # 人工校准理由

  "task_ancient_minutes": 120.0, # 单位分钟，传统开发时长预估 完成当前task中的需求和产出类似结果需要的时间 通过聚合task/conversation/2026/04/01/$task_id.json中的"user_input"，结合task/summary/2026/04/01/$task_id.json 中的diff_lines和diff数据让AI预估 传统开发时长预估 所需时间（分钟）
  "task_ancient_minutes_reason": "该任务涉及3个Vue组件修改、2个Go API接口开发、数据库schema变更，传统编码预计需要约2小时",
  "task_ancient_minutes_manual": null, # 人工校准值
  "task_ancient_minutes_reason_manual": null, # 人工校准理由

  "efficiency_ratio": 0.3142 # 提效比 = (task_ancient_minutes / task_real_minutes)，优先使用 _manual 字段。表示AI辅助编码相比 传统开发时长预估 的效率倍数（百分比）。800%(无小数) 表示效率是传统开发时长预估 的8倍。
}
```

通过/task/task_id 页面展示task的详情信息，其中包含跳转关联关系
1. 在Task详情展示页面，可以通过点击 work_dir_id 跳转到Work_dir的详情页面。
2. 在Task详情展示页面，可以通过点击 repo_addr 跳转到 path-safe(repo_addr)/path-safe(repo_branch)详情页面
3. 在Task详情展示页面，可以通过点击user_id跳转到 用户的详情页面


### task_real_minutes 计算算法

**目标**：基于 task_conversation.jsonl 中的 request 时间序列，计算用户实际编码耗时。

**全局配置参数**（在 backend/config.yaml 中配置）：
- `gap_threshold_minutes`：默认 30，相邻对话间隔超过此值则视为「断开」
- `extension_minutes`：默认 5，每个连续时间片段的末尾追加此延长值

**算法步骤**：
1. 提取所有 conversation 的 `start_time`，过滤掉 null 值
2. 按 `start_time` 升序排序
3. 初始化第一个时间片段：`segment_start = times[0]`
4. 遍历 `times[1:]`：
   - 计算当前时间与上一个时间的间隔 `gap = times[i] - times[i-1]`
   - 如果 `gap <= gap_threshold_minutes`：归入当前片段，继续
   - 如果 `gap > gap_threshold_minutes`：
     - 结束当前片段：`segment_end = times[i-1] + extension_minutes`
     - 记录当前片段：`[segment_start, segment_end]`
     - 开始新片段：`segment_start = times[i]`
5. 最后一个片段：`segment_end = times[last] + extension_minutes`
6. 累加所有片段的时长：`sum(segment_end - segment_start)`
7. 生成 reason 字符串描述各片段起止时间

**示例**：
- 对话时间序列：`[10:20:59, 10:24:59, 12:34:59, 12:35:59]`
- gap_threshold=30分钟，extension=5分钟
- 片段1：`10:20:59 ~ 10:29:59`（10:24:59 + 5分钟延长 = 10:29:59），时长 = 9.0 分钟
- 间隔：`12:34:59 - 10:24:59 = 130 分钟 > 30 分钟`，断开
- 片段2：`12:34:59 ~ 12:40:59`（12:35:59 + 5分钟延长 = 12:40:59），时长 = 6.0 分钟
- **task_real_minutes = 9.0 + 6.0 = 15.0 分钟**


### efficiency_ratio 计算逻辑

```
effective_ancient = task_ancient_minutes_manual ?? task_ancient_minutes
effective_real = task_real_minutes_manual ?? task_real_minutes

if effective_ancient > 0 AND effective_real > 0:
    efficiency_ratio = (effective_ancient / effective_real) * 100
else:
    efficiency_ratio = null
```

### formatDuration 显示规则

根据分钟数自适应显示：
- < 60 分钟：显示 "X分钟"（如 "15分钟"）
- 60~480 分钟：显示 "X小时Y分钟"（如 "2小时15分钟"）
- > 480 分钟（1人天=8小时=480分钟）：显示 "X.X人天"（如 "1.5人天"）

---

服务端程序分析完成后会把文件mv到task/analysed/目录，task/summary/中对应文件会被删除。这样确保下次扫描比较快捷。


# commit数据

## 全局 ID path-safe 格式

repo_id 和 project_id 统一使用 **path-safe 格式**，方便 URL 传递和目录创建。

**转换规则**：原始值 → 转小写 → 非(小写字母/数字/-)字符替换为 `-` → 去除首尾 `-` → 合并连续 `-`

**示例**：
| 原始值 | path-safe 格式 |
|--------|---------------|
| `https://github.com/zgsm-ai/costrict.git-main` | `https-github-com-zgsm-ai-costrict-git-main` |
| `797e102c29:d:\projects\creditControl\credit-sentinel` | `797e102c29-d-projects-creditcontrol-credit-sentinel` |

## 客户端

建议客户端存储路径为：`repo/$repo_addr/$repo_branch/2026/0401-$commit_id.json`（2026/0401 为 commit_time 的年月日）。其中 `repo_addr` `repo_branch` 都是经过path-safe格式化后的之后才。

客户端上传上来的 `$commit_id.json` 字段示例：
```json
{
  "commit_id": "a1b2c3d4e5f6...",
  "commit_time": "2026-03-29T10:07:04Z",
  "repo_addr": "https://github.com/zgsm-ai/costrict.git",
  "repo_branch": "main",
  "git_user_name": "zhangsan",
  "git_user_email": "zhangsan@example.com",
  
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "work_path": "d:\\project\\costrict-all\\user-indicator-collection", # 之前叫project_path，修改了名称
  
  "comment": "xxx" #把comment取出来放到这里，最大支持150个字符，超出截断
  "diff_lines": 1494,
  "diff": ""
}
```

## 服务端

### 存储commits 表（数据库字段定义）
服务端的数据库叫costrict_stat，密码1，后续所有的表都存储到这个数据下
在`$commit_id.json`上删除diff字段，并新增如下字段，然后存储到commits表中：

```json
{
  "commit_ancient_minutes": 120.0,
  // 单位分钟。传统开发时长预估 完成当前 commit 中代码变更需要的时间。
  // 预估方法：根据 commit 的 diff_lines 和 diff 内容，由 AI 分析代码复杂度和工作量后给出。
  // 字段长度：DECIMAL(10,2)
  
  "commit_ancient_minutes_reason": "该commit包含3个文件修改共150行...",
  // AI 预估时间的理由说明，最大200字符。
  // 字段类型：TEXT
  
  "commit_ancient_minutes_manual": null,
  // 人工校准的 传统开发时长预估 值（分钟）。当用户认为 AI 预估不准确时手动填写。
  // 计算优先级：commit_ancient_minutes_manual ?? commit_ancient_minutes
  
  "commit_ancient_minutes_reason_manual": null,
  // 人工校准的理由说明。

  "task_ids": ["019d3914-eee8-74df-83c6-481b72521f89", "019d3a00-..."],
  // 关联的 task_id 数组（JSONB）。
  // 关联条件：
  //   1. commit.repo_id == task.repo_id（同一仓库）
  //   2. commit.user_id == task.user_id（同一用户）
  //   3. task.start_time < commit.commit_time（task 开始早于 commit）
  //   4. commit.commit_time < task.start_time + 7days（commit 在 task 开始后7天内）
  // 初步关联后，通过 AI 分析 task.diff 和 commit.diff 的相似度，确认硅比例task_ids_silica

  "task_ids_silica": [0.78, 0.22],
  // 与 task_ids 一一对应的硅比例数组（JSONB）。
  // silica = 0.78 表示该 Task 贡献了此 commit 78% 的代码。
  // 由 AI 分析 task.diff 和 commit.diff 的包含/相似关系得出
  
  "upstream_tokens": 434, # 输入给AI的tokens数量，来自 所有task_ids.upstream_tokens*task_ids_silica的累加 
  "downstream_tokens": 4342, # AI输出的tokens数量，来自 所有task_ids.downstream_tokens*task_ids_silica的累加 
  
  "cost": 0.13 #所有task_ids.cost*task_ids_silica的累加
  "silica": 0.32 # 可以通过关联Task来计算出来：a) 获得关联task的diff b) 获得自己commit的diff 通过AI对比分析diff差异，得出：commit_ancient_minutes commit_ancient_minutes_reason
、silica 这就是说当前commit多少代码是由task生成的，如果全部是由task生成的就是100%，反之全部没有关系就是0%。

  "commit_real_ai_minutes":20.5, # commit_real_ai_minutes = sum(task_ids[i].task_real_minutes * task_ids_silica[i]) for all i
  "commit_real_ancient_minutes":25, # commit_real_ancient_minutes = sum(task_ids[i].task_ancient_minutes * (1-task_ids_silica[i])) for all i
  "commit_real_minutes": 45.5, #// 实际耗时（分钟）commit_real_minutes = commit_real_ai_minutes + commit_real_ancient_minutes
  //如果 task_ids 为空 commit_real_ai_minutes 为 0，表示没有AI参与，而commit_real_ancient_minutes=commit_ancient_minutes就是预估的时间
  //如果task_ids task_ids_silica 有更新，commit_real_ai_minutes commit_real_ancient_minutes commit_real_minutes值也应该更新
  
  "commit_real_minutes_reason": "把计算逻辑写清楚，比如commit_real_ai_minutes怎么计算来的，commit_real_ancient_minutes怎么计算来的 计算逻辑说明，最大200字符。",

  "commit_real_minutes_manual": null,
  // 人工校准的实际耗时（分钟）。
  // 计算优先级：commit_real_minutes_manual ?? commit_real_minutes
  
  "commit_real_minutes_reason_manual": null // 人工校准的理由说明。
}
```

### efficiency_ratio 计算逻辑（commit 级别）

```
effective_ancient = commit_ancient_minutes_manual ?? commit_ancient_minutes
effective_real = commit_real_minutes_manual ?? commit_real_minutes

if effective_ancient > 0 AND effective_real > 0:
    efficiency_ratio = (effective_ancient / effective_real) * 100
else:
    efficiency_ratio = null
```

通过/commit/commit-id 页面展示commit的详情信息

# repo 表
服务端的数据库叫costrict_stat，密码1，后续所有的表都存储到这个数据下
```
{
  "repo_addr": "https://github.com/zgsm-ai/costrict.git", 
  "repo_branch": "main", 
  "start_time": "2026-03-29T10:07:04Z", # repo所有commits 中最早的commit_time
  "end_time": "2026-03-29T10:11:57Z", # repo所有commits 中最晚的commit_time
  "commit_ids":["xxxx"] #记录所有commit id，方便查找。
}
```
可以基于这个信息生成repo页面，把所有repo以表格形式列出来，表格支持条件筛选过滤。

## repodetail页面
其内容应该是来自于commits表，按repo_addr/repo_branch加上start_time end_time过滤出来的

repo 详情页面 路径 /repo/path-safe(repo_addr)：
1. 用户可以下拉 repo_branch 到不同的分支页面去。对应路径修改为： /repo/path-safe(repo_addr)/path-safe(repo_branch)
2. 在repo_branch分支页面，支持筛选时间段，用来筛选start_time和end_time
3. 在repo_branch分支页面，列出来在start_time到end_time之间的所有repo_branch下的commits条目信息（即include_commits），通过点击这些条目，可以跳转到 /commit/commit_id 页面
4. 在repo_branch分支页面，列出来在start_time到end_time之间的所有repo_branch下的commits.task_ids 条目信息（根据include_commits下钻到task_ids获取），通过点击这些条目，可以跳转到 /task/task_id 页面
5.  在repo_branch分支页面，列出来在start_time到end_time之间效率评估，主要是这些字段：
```
  //如下主要解决 统开发时长预估 问题
  # 根据用户选择的start_time到end_time，筛选commit_ids
  # repo_ancient_minutes = sum(commit_ancient_minutes)
  "repo_ancient_minutes": 120.0,
  // 单位分钟。传统开发时长预估 完成当前 commit 中代码变更需要的时间。
  // 预估方法：根据 commit 的 diff_lines 和 diff 内容，由 AI 分析代码复杂度和工作量后给出。
  // 字段长度：DECIMAL(10,2)
  
  "repo_ancient_minutes_reason": "该commit包含3个文件修改共150行...",
  // AI 预估时间的理由说明，最大200字符。
  // 字段类型：TEXT
  
  "repo_ancient_minutes_manual": null,
  // 人工校准的传统开发时长预估 值（分钟）。当用户认为 AI 预估不准确时手动填写。
  // 计算优先级：commit_ancient_minutes_manual ?? commit_ancient_minutes
  
  "repo_ancient_minutes_reason_manual": null,
  // 人工校准的理由说明。
  
  //如下主要解决真实时长问题
  # repo_real_minutes = sum(commit_real_minutes)
  "repo_real_minutes": 10.0,
  
  "repo_real_minutes_reason": "task1: 30min*0.78=23.4 + task2: 100min*0.22=22.0 = 45.4min",
  // 计算逻辑说明，最大200字符。
  
  "repo_real_minutes_manual": null,
  // 人工校准的实际耗时（分钟）。
  // 计算优先级：repo_real_minutes_manual ?? repo_real_minutes
  
  "repo_real_minutes_reason_manual": null
  // 人工校准的理由说明。
  
  
  "efficiency_ratio": 270% 提效比例
```



---------------

# project数据
服务端的数据库叫costrict_stat，密码1，后续所有的表都存储到这个数据下
project的数据可能来自repos过滤的commits，也可能来自task_ids指定的task，两者的和。

这里task_ids如何添加？可以从task页面去过滤条件，比如过滤某个client_id和work_dir的内容，或时间的内容，然后多选或全选，添加到某个project
repos怎么添加，也可以在repos页面，点击到某个repodetail子页面，在此子页面选择添加某个时间范围或者某些白名单commits（include_only_commits）或排除黑名单commits （exclude_commits），添加到某个project，注意，要提示冲突情况，比如一个commit既在project A中又在project B中的时候，需要提示用户。

最终得到一条project的数据（如下示例），可以创建一个projects表来存储这些数据：
```
{
  "project_id": "uuid",
  
    "repos"[
    {
      # 这是一个Repo的过滤条件，即基于如下条件过滤commits表，归并出来的结果。
      "repo_addr": "https://github.com/zgsm-ai/costrict.git"
      "repo_branch":"main"
      "start_time":"2026-03-29T10:07:04Z" 支持时间是null的情况，表示从最开始计算
      "end_time": "2026-03-29T10:11:57Z" 支持时间是null的情况，表示到当前时间
      "exclude_commits":["commit-id1"] # 这里可以配置排除在外的commits
      "include_only_commits":["commit-id1"] # 这里可以配置白名单commits，只包含这些commits，这个有值的话，则白名单生效，exclude_commits失效；真正的include_commits = include_only_commits 或者 (在start_time到end_time之间的所有commits 减去 exclude_commits)。
    }
    ... 一个project可以添加多个repo，还可以添加tasks
  ]
  "task_ids":["019d3914-eee8-74df-83c6-481b72521f89",  "019d3914-eee8-74df-83c6-481b72521f89",  "019d3914-eee8-74df-83c6-481b72521f89"]
  "task_ids_silica": [0.78,  0.01,  1.0]
  
  //repos task_ids task_ids_silica等信息发生变化的时候，下面的信息很多都要跟着变更，比如start_time end_time upstream_tokens cost project_ancient_minutes project_real_process_minutes project_real_lead_minutes等等
  
  "start_time": "2026-03-29T10:07:04Z", # 工程的开始时间 根据task_ids repos 中最早的start_time作为这个起始时间
  "end_time": "2026-03-29T10:11:57Z", # 工程的结束时间，以最晚的end_time作为结束时间
  "start_time_manual": "2026-03-29T10:07:04Z", # 工程的开始时间 用户配置,可以为空，
  "end_time_manual": "2026-03-29T10:11:57Z", # 工程的结束时间，用户配置, 可以为空
  

  "upstream_tokens": 434 # 整个project相关的task_ids累计的tokens，记住，repos中commits.task_ids和task_ids需要去重。
  "downstream_tokens": 4342 # 同上
  "cost": 1.43 # 累计的成本
  
  "project_ancient_minutes": 120.0 # commits和task_ids中的 传统开发时长预估 总和
  "project_ancient_minutes_reason": "" #
  
  "project_ancient_minutes_manual": 120.0 # 人工修正值（分钟）
  "project_ancient_minutes_reason_manual": "" # 人工修正理由
  
  "project_real_process_minutes": 120.0 # commits和task_ids中的真实process_time累加
  "project_real_process_minutes_reason": "" # 
  
  "project_real_process_minutes_manual": 120.0 
  "project_real_process_minutes_reason_manual": "" # 
  
  "project_real_lead_minutes": 120.0 # 这个直接用end_time - start_time即可
  "project_real_lead_minutes_reason": "" # 
  
  "project_real_lead_minutes_manual": 120.0 
  "project_real_lead_minutes_reason_manual": "" # 
}
```

最后要展示提效比例。



# user_productivity 表
服务端的数据库叫costrict_stat，密码1，后续所有的表都存储到这个数据下
user_productivity表，基于task/summary/2026/04/01/$task_id.json的内容，每天归档为一条user的数据
基于repo/$repo_addr/$repo_branch/2026/0401-$commit_id.json 按天归并到user的数据中
```
{
  "user_productivity_id": "a1b2c3d4e5f6-20260321" # 这个就是 user_id + 日期作为主键的
  "create_time": "2026-03-13 21:31:41" #记录的创建时间
  
  //如下记录来自并自task/summary/2026/04/01/$task_id.json
  "user_id": "a1b2c3d4e5f6...", 
  "user_name": "15852591920",
  
  //相关的task_ids
  "task_ids":["019d3914-eee8-74df-83c6-481b72521f89",  "019d3914-eee8-74df-83c6-481b72521f89",  "019d3914-eee8-74df-83c6-481b72521f89"]

  //和这个用户相关的work_dir_id，
  "work_dir_id": ["7fc2b9a3bd58717380e501471c69e48b1cd2ab7b..."],
  
  //task_ids相关联diff_lines值的累加
  "task_diff_lines": 1494,

  "upstream_tokens": 434, //task_ids相关联task值的累加
  "downstream_tokens": 4342,//task_ids相关联task值的累加
  "cost": 1.43, //task_ids相关联task值的累加

  "task_real_minutes": 12.5, # //task_ids相关联task值的累加 任务实际耗时(分钟)，基于对话时间序列计算
  "task_ancient_minutes": 120.0, //task_ids相关联task值的累加

  "task_efficiency_ratio": 0.3142 # 提效比 = (task_ancient_minutes / task_real_minutes)
  
  //如下数据来自repo/$repo_addr/$repo_branch/2026/0401-$commit_id.json归并
  "commit_ids": ["a1b2c3d4e5f6..."],
  
  //commit_ids相关联diff_lines等值的累加
  "commit_diff_lines": 1494,
  "commit_ancient_minutes": 120.0,
  "commit_real_ai_minutes":20.5,
  "commit_real_ancient_minutes":25, 
  "commit_real_minutes": 45.5,
  "commit_efficiency_ratio": 0.3142 # 提效比 = (commit_ancient_minutes / commit_real_minutes)
}
```

通过上面的数据，可以展示userProductivityDetail页面，按天查看也行，汇总起来也行。

整个表也可以支撑起userProductiviy页面，以user_id视角归并查看。

基于这个userProductiviy页面，多选择多个user_id到一起，创建虚拟组。


# org
org视图。一定能够要把UI设计逻辑合理。
服务端的数据库叫costrict_stat，密码1，后续所有的表都存储到这个数据下

一个用户属于某一个组织，那我希望基于user_productivity，按天把这个组织的信息展示出来，把不需要按天显示的东西归并到一起。并且可以筛选日期范围和组。
支持创建虚拟组，即把某几个用户创建成一个虚拟组，这个组也可以在org视图展示；原本的org视图其实是多级org结构  org1 org2 org3 org4这个筛选依然需要，只是多了一个虚拟组没有org多级结构


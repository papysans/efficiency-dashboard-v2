# efficiency-dashboard设计

## 设计原理

本系统旨在通过收集和分析AI辅助编程过程中的数据，量化评估AI对开发效率的提升。系统通过客户端采集task对话数据和commit代码变更数据，结合AI分析技术，计算传统开发时长预估与实际耗时的对比，从而得出提效比。

核心设计理念：

- **数据驱动**：基于真实的对话记录和代码变更数据进行分析
- **时间序列分析**：通过对话时间序列计算实际耗时，考虑工作片段的连续性
- **AI辅助评估**：利用AI分析代码复杂度和工作量，预估传统开发所需时长
- **多维度关联**：建立task、commit、repo、project之间的关联关系，支持多维度效率分析
- **人工校准机制**：提供人工修正字段，确保数据准确性

## 数据格式

### 客户端上报格式

#### task_conversation.jsonl

记录每次request对话，以用户能看到的视角进行append方式记录。建议存储路径：`task/conversation/2026/04/01/$task_id.json`（2026/04/01为task start time的年月日）。

```json
{
  "sender": "user",
  "request_id": "019d4288-88b6-735b-ab94-3dd61b39dc48",
  "prompt_mode": "vibe",
  "mode": "code",
  "model": "GLM-4.7",
  "start_time": "2026-03-29T10:07:04Z",
  "end_time": "2026-03-29T10:11:57Z",
  "process_time": 8507,
  "process_ttft": 8422,
  "upstream_tokens": 434,
  "downstream_tokens": 4342,
  "cost": 1.43,
  "request_content": "不管是用户输入的，还是agent输入的，都放到这里",
  "response_content": "AI API返回的内容",
  "user_input": "如果sender 是user，则提取request_content中用户输入内容到这里",
  "diff": "",
  "diff_lines": 1494,
  "error_code": 231,
  "error_reason": "如果有错误，则这里是错误描述"
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| sender | string | 消息发送方标识 | 客户端直接记录，取值：`"user"`（用户本人发送）或 `"agent"`（AI代理自动发起） |
| request_id | string | 请求唯一标识 | 客户端生成的 UUID |
| prompt_mode | string | 提示词模式 | 客户端直接记录，如 `"vibe"` 表示氛围模式 |
| mode | string | 对话模式 | 客户端直接记录，如 `"code"` 表示代码模式 |
| model | string | 使用的AI模型名称 | 客户端直接记录，如 `"GLM-4.7"` |
| start_time | string (ISO 8601) | 请求开始时间 | 客户端记录请求发起的时间戳 |
| end_time | string (ISO 8601) | 请求结束时间 | 客户端记录AI响应完成的时间戳 |
| process_time | number | AI处理总耗时（毫秒） | `end_time - start_time`，由客户端计算 |
| process_ttft | number | 首个Token返回耗时（毫秒） | 客户端记录从请求发出到首个Token返回的时间差 |
| upstream_tokens | number | 上行Token数（输入） | AI API返回的prompt token用量 |
| downstream_tokens | number | 下行Token数（输出） | AI API返回的completion token用量 |
| cost | number | 本次请求费用（元） | 根据模型单价和token用量计算 |
| request_content | string | 请求内容 | 包含用户输入和agent输入的完整请求内容 |
| response_content | string | 响应内容 | AI API返回的完整内容 |
| user_input | string | 用户实际输入内容 | 当 `sender == "user"` 时，从 `request_content` 中提取 |
| diff | string | 本次对话产生的代码变更 | 客户端记录，支持三种diff格式 [详见](#diff格式说明) |
| diff_lines | number | 变更行数 | 客户端统计diff文本的行数 |
| error_code | number | 错误码 | AI API返回的错误码，无错误时为0或null |
| error_reason | string | 错误描述 | 当发生错误时的详细描述 |

#### task_summary.json

基于task_conversation.jsonl生成的汇总数据。建议存储路径：`task/summary/2026/04/01/$task_id.json`。

```json
{
  "task_id": "019d3914-eee8-74df-83c6-481b72521f89",
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "client_ide": "vscode",
  "client_version": "2.5.3",
  "client_os": "Windows",
  "client_os_version": "7.1.323",
  "caller": "chat",
  "repo_addr": "https://github.com/zgsm-ai/costrict.git",
  "repo_branch":"main",
  "work_dir": "d:\\project\\costrict-all\\user-indicator-collection",
  "diff": "",
  "diff_lines": 1494
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| task_id | string | 任务唯一标识 | 客户端生成的 UUID |
| user_id | string | 用户唯一标识 | 客户端从认证系统获取 |
| user_name | string | 用户名称 | 客户端从认证系统获取 |
| client_id | string | 客户端唯一标识 | 客户端安装时生成的设备指纹hash |
| client_ide | string | 客户端IDE类型 | 客户端直接记录，如 `"vscode"` |
| client_version | string | 客户端版本号 | 客户端直接记录，如 `"2.5.3"` |
| client_os | string | 客户端操作系统 | 客户端直接记录，如 `"Windows"` |
| client_os_version | string | 客户端操作系统版本 | 客户端直接记录，如 `"7.1.323"` |
| caller | string | 调用来源 | 客户端直接记录，如 `"chat"` 表示聊天界面触发 |
| repo_addr | string | 仓库地址 | 客户端从当前工作区Git配置获取 |
| repo_branch | string | 仓库分支 | 客户端从当前工作区Git配置获取 |
| work_dir | string | 工作目录路径 | 客户端记录当前工作目录的绝对路径 |
| diff | string | 任务整体代码变更 | 合并该task所有conversation的diff（去除重复变更，保留最终结果） |
| diff_lines | number | 任务整体变更行数 | 合并后diff的行数统计 |

#### commit数据

建议存储路径：`repo/$repo_addr/$repo_branch/2026/04/01/$commit_id.json`（2026/04/01为commit_time的年月日）。其中`repo_addr`和`repo_branch`都经过path-safe格式化。

```json
{
  "commit_id": "a1b2c3d4e5f6...",
  "commit_time": "2026-03-29T10:07:04Z",
  "repo_addr": "https://github.com/zgsm-ai/costrict.git",
  "repo_branch": "main",
  "git_user_name": "zhangsan",
  "git_user_email": "zhangsan@example.com",
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "work_dir": "d:\\project\\costrict-all\\user-indicator-collection",
  "comment": "xxx",
  "diff_lines": 1494,
  "diff": ""
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| commit_id | string | Git提交的唯一哈希 | 客户端从Git获取 |
| commit_time | string (ISO 8601) | 提交时间 | 客户端从Git获取的提交时间戳 |
| repo_addr | string | 仓库地址 | 客户端从Git配置获取 |
| repo_branch | string | 仓库分支 | 客户端从Git配置获取 |
| git_user_name | string | Git用户名 | 客户端从Git配置获取 |
| git_user_email | string | Git用户邮箱 | 客户端从Git配置获取 |
| user_id | string | 用户唯一标识 | 客户端从认证系统获取，关联git用户和系统用户 |
| user_name | string | 用户名称 | 客户端从认证系统获取 |
| client_id | string | 客户端唯一标识 | 客户端安装时生成的设备指纹hash |
| work_dir | string | 工作目录路径 | 客户端记录当前工作目录 |
| comment | string | 提交注释 | Git commit message |
| diff_lines | number | 变更行数 | 客户端统计commit的diff行数 |
| diff | string | 代码变更内容 | `git show --format= --diff-filter=ACDMR <commit_id>` 获取 [详见](#diff格式说明) |

#### diff格式 {#diff格式说明}

task_conversation.jsonl、task_summary.json和commit数据中的diff字段，表示代码变更内容，该数据可能采用下述三种格式：

1. Git Unified Diff格式

采用下述命令输出的diff文本（Unified Diff格式）：

```
git show --format= --diff-filter=ACDMR <commit_id>
```

> `--format=` 为空值，作用是抑制输出commit元信息（作者、日期、提交说明等），仅输出diff正文。

2. JSON格式：

当diff字段采用JSON格式时，为数组序列化文本，每个元素的结构如下：

```
{
  file: string,        // 文件路径
  before: string,      // 变更前内容
  after: string,       // 变更后内容
  additions: number,   // 新增行数
  deletions: number,   // 删除行数
  status?: "added" | "deleted" | "modified"  // 变更状态
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| file | string | 文件路径 | 变更文件的相对路径 |
| before | string | 变更前内容 | 文件修改前的完整内容 |
| after | string | 变更后内容 | 文件修改后的完整内容 |
| additions | number | 新增行数 | 统计after相对before新增的行数 |
| deletions | number | 删除行数 | 统计before相对after删除的行数 |
| status | string (可选) | 变更状态 | `"added"` / `"deleted"` / `"modified"` |

3. 简化diff格式

以"<<< BEFORE"和">>> AFTER"分隔修改前后的内容。
"<<< BEFORE"和">>> AFTER"之间的是修改前内容，">>> AFTER"到末尾的内容是修改后的内容。

```
--- xdr-incident-core/src/main/java/com/sangfor/ngsoc/incident/core/dao/IIncidentDao.java
<<< BEFORE
修改前内容
>>> AFTER
修改后内容
```

### 后端数据表格式

#### tasks表

数据库：costrict_stat
表：tasks

```json
{
  //来自task_summary.json
  "task_id": "019d3914-eee8-74df-83c6-481b72521f89",
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "client_ide": "vscode",
  "client_version": "2.5.3",
  "client_os": "Windows",
  "client_os_version": "7.1.323",
  "caller": "chat",
  "repo_addr": "https://github.com/zgsm-ai/costrict.git",
  "repo_branch":"main",
  "work_dir": "d:\\project\\costrict-all\\user-indicator-collection",
  "diff_lines": 1494,
  //计算生成
  "work_dir_id": "7fc2b9-d-project-costrict-all-user-indicator-collection",
  "start_time": "2026-03-29T10:07:04Z",
  "end_time": "2026-03-29T10:11:57Z",
  "upstream_tokens": 434,
  "downstream_tokens": 4342,
  "cost": 1.43,
  "title": "这是一个什么任务",
  "task_real_minutes": 12.5,
  "task_real_minutes_reason": "片段1: 10:20:59~10:29:59 (4.0分钟+5.0分钟延长) | 片段2: 12:34:59~12:40:59 (1.0分钟+5.0分钟延长) | 合计=15.0分钟",
  "task_real_minutes_manual": null,
  "task_real_minutes_reason_manual": null,
  "task_ancient_minutes": 120.0,
  "task_ancient_minutes_reason": "该任务涉及3个Vue组件修改、2个Go API接口开发、数据库schema变更，传统编码预计需要约2小时",
  "task_ancient_minutes_manual": null,
  "task_ancient_minutes_reason_manual": null,
  "efficiency_ratio": 0.3142
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| task_id | string | 任务唯一标识（主键） | 来自 task_summary.json |
| user_id | string | 用户唯一标识 | 来自 task_summary.json |
| user_name | string | 用户名称 | 来自 task_summary.json |
| client_id | string | 客户端唯一标识 | 来自 task_summary.json |
| client_ide | string | 客户端IDE类型 | 来自 task_summary.json |
| client_version | string | 客户端版本号 | 来自 task_summary.json |
| client_os | string | 客户端操作系统 | 来自 task_summary.json |
| client_os_version | string | 客户端操作系统版本 | 来自 task_summary.json |
| caller | string | 调用来源 | 来自 task_summary.json |
| repo_addr | string | 仓库地址 | 来自 task_summary.json |
| repo_branch | string | 仓库分支 | 来自 task_summary.json |
| work_dir | string | 工作目录路径 | 来自 task_summary.json |
| diff_lines | number | 变更行数 | 来自 task_summary.json |
| work_dir_id | string | 工作目录path-safe标识 | 对 `work_dir` 做path-safe格式化 [详见](#path-safe格式化规则) |
| start_time | string (ISO 8601) | 任务开始时间 | 取所有conversation中最早的 `start_time` |
| end_time | string (ISO 8601) | 任务结束时间 | 取所有conversation中最晚的 `end_time` |
| upstream_tokens | number | 上行Token总数 | 汇总所有conversation的 `upstream_tokens` |
| downstream_tokens | number | 下行Token总数 | 汇总所有conversation的 `downstream_tokens` |
| cost | number | 总费用（元） | 汇总所有conversation的 `cost` |
| title | string | 任务标题/描述 | AI分析conversation内容提取 [详见](#传统开发时长预估) |
| task_real_minutes | number | 任务实际耗时（分钟） | 时间序列片段算法 [详见](#task_real_minutes计算算法) |
| task_real_minutes_reason | string | 实际耗时计算说明 | 自动生成，描述各片段起止时间和延长值 |
| task_real_minutes_manual | number (可null) | 人工校准的实际耗时 | 人工修正值，null时用自动计算值 [详见](#人工校准机制) |
| task_real_minutes_reason_manual | string (可null) | 人工校准耗时说明 | 人工修正时填写的说明 |
| task_ancient_minutes | number | 传统开发预估时长（分钟） | diff_lines粗估 + AI校准 [详见](#传统开发时长预估) |
| task_ancient_minutes_reason | string | 传统预估时长说明 | AI生成的分析理由 |
| task_ancient_minutes_manual | number (可null) | 人工校准的传统开发时长 | 人工修正值，null时用AI预估值 [详见](#人工校准机制) |
| task_ancient_minutes_reason_manual | string (可null) | 人工校准传统时长说明 | 人工修正时填写的说明 |
| efficiency_ratio | number | 提效比 | 传统时长/实际耗时×100 [详见](#efficiency_ratio计算逻辑) |

#### commits表

数据库：costrict_stat
表：commits

```json
{
   //来自$commit_id.json
  "commit_id": "a1b2c3d4e5f6...",
  "commit_time": "2026-03-29T10:07:04Z",
  "repo_addr": "https://github.com/zgsm-ai/costrict.git",
  "repo_branch": "main",
  "git_user_name": "zhangsan",
  "git_user_email": "zhangsan@example.com",
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920",
  "client_id": "7fc2b9a3bd58717380e501471c69e48b1cd2ab7b...",
  "work_dir": "d:\\project\\costrict-all\\user-indicator-collection",
  "comment": "xxx",
  "diff_lines": 1494,
   //计算而得
  "commit_ancient_minutes": 120.0,
  "commit_ancient_minutes_reason": "该commit包含3个文件修改共150行...",
  "commit_ancient_minutes_manual": null,
  "commit_ancient_minutes_reason_manual": null,
  "task_ids": ["019d3914-eee8-74df-83c6-481b72521f89"],
  "task_ids_silica": [0.78],
  "upstream_tokens": 434,
  "downstream_tokens": 4342,
  "cost": 0.13,
  "silica": 0.32,
  "commit_real_ai_minutes": 20.5,
  "commit_real_ancient_minutes": 25,
  "commit_real_minutes": 45.5,
  "commit_real_minutes_reason": "把计算逻辑写清楚",
  "commit_real_minutes_manual": null,
  "commit_real_minutes_reason_manual": null
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| commit_id | string | Git提交哈希（主键） | 来自 commit 数据 |
| commit_time | string (ISO 8601) | 提交时间 | 来自 commit 数据 |
| repo_addr | string | 仓库地址 | 来自 commit 数据 |
| repo_branch | string | 仓库分支 | 来自 commit 数据 |
| git_user_name | string | Git用户名 | 来自 commit 数据 |
| git_user_email | string | Git用户邮箱 | 来自 commit 数据 |
| user_id | string | 用户唯一标识 | 来自 commit 数据 |
| user_name | string | 用户名称 | 来自 commit 数据 |
| client_id | string | 客户端唯一标识 | 来自 commit 数据 |
| work_dir | string | 工作目录路径 | 来自 commit 数据 |
| comment | string | 提交注释 | 来自 commit 数据 |
| diff_lines | number | 变更行数 | 来自 commit 数据 |
| commit_ancient_minutes | number | 传统开发预估时长（分钟） | diff_lines粗估 + AI校准 [详见](#传统开发时长预估) |
| commit_ancient_minutes_reason | string | 传统预估时长说明 | AI生成的分析理由 |
| commit_ancient_minutes_manual | number (可null) | 人工校准的传统开发时长 | 人工修正值，null时用AI预估值 [详见](#人工校准机制) |
| commit_ancient_minutes_reason_manual | string (可null) | 人工校准传统时长说明 | 人工修正时填写的说明 |
| task_ids | string[] | 关联的任务ID列表 | 同仓库+同用户+时间窗口匹配 [详见](#commit关联task算法) |
| task_ids_silica | number[] | 各关联task的贡献比例 | 与task_ids一一对应，diff相似度算法 [详见](#commit关联task算法) |
| upstream_tokens | number | 上行Token总数 | 汇总关联task的upstream_tokens |
| downstream_tokens | number | 下行Token总数 | 汇总关联task的downstream_tokens |
| cost | number | 总费用（元） | 汇总关联task的cost |
| silica | number | AI辅助占比 | 所有关联task的silica比例之和 |
| commit_real_ai_minutes | number | AI辅助实际耗时（分钟） | [详见](#commit耗时计算) |
| commit_real_ancient_minutes | number | 非AI部分传统开发时长（分钟） | [详见](#commit耗时计算) |
| commit_real_minutes | number | commit总实际耗时（分钟） | [详见](#commit耗时计算) |
| commit_real_minutes_reason | string | 总实际耗时计算说明 | 自动生成，描述各task贡献和计算过程 |
| commit_real_minutes_manual | number (可null) | 人工校准的总实际耗时 | 人工修正值，null时用自动计算值 [详见](#人工校准机制) |
| commit_real_minutes_reason_manual | string (可null) | 人工校准耗时说明 | 人工修正时填写的说明 |

#### projects表

数据库：costrict_stat
表：projects
用途：以项目维度汇总数据，支持多仓库组合。

```json
{
  "project_id": "uuid",
  "repos": [
    {
      "repo_addr": "https://github.com/zgsm-ai/costrict.git",
      "repo_branch":"main",
      "start_time":"2026-03-29T10:07:04Z",
      "end_time": "2026-03-29T10:11:57Z",
      "exclude_commits":["commit-id1"],
      "include_only_commits":["commit-id1"]
    }
  ],
  "task_ids":["019d3914-eee8-74df-83c6-481b72521f89"],
  "task_ids_silica": [0.78],
  "start_time": "2026-03-29T10:07:04Z",
  "end_time": "2026-03-29T10:11:57Z",
  "start_time_manual": "2026-03-29T10:07:04Z",
  "end_time_manual": "2026-03-29T10:11:57Z",
  "upstream_tokens": 434,
  "downstream_tokens": 4342,
  "cost": 1.43,
  "project_ancient_minutes": 120.0,
  "project_ancient_minutes_reason": "",
  "project_ancient_minutes_manual": 120.0,
  "project_ancient_minutes_reason_manual": "",
  "project_real_process_minutes": 120.0,
  "project_real_process_minutes_reason": "",
  "project_real_process_minutes_manual": 120.0,
  "project_real_process_minutes_reason_manual": "",
  "project_real_lead_minutes": 120.0,
  "project_real_lead_minutes_reason": "",
  "project_real_lead_minutes_manual": 120.0,
  "project_real_lead_minutes_reason_manual": ""
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| project_id | string | 项目唯一标识（主键） | 系统生成的 UUID |
| repos | object[] | 关联的仓库列表 | 用户配置的仓库及分支组合，含子字段如下 |
| repos[].repo_addr | string | 仓库地址 | 用户配置 |
| repos[].repo_branch | string | 仓库分支 | 用户配置 |
| repos[].start_time | string (ISO 8601) | 仓库起始时间 | 用户配置或自动获取 |
| repos[].end_time | string (ISO 8601) | 仓库截止时间 | 用户配置或自动获取 |
| repos[].exclude_commits | string[] | 排除的commit列表 | 用户配置，排除不需要计入的commit |
| repos[].include_only_commits | string[] | 仅包含的commit列表 | 用户配置，限定只计入指定的commit |
| task_ids | string[] | 关联的任务ID列表 | 项目下所有仓库关联的task汇总 |
| task_ids_silica | number[] | 各task的silica比例 | 与task_ids一一对应，来自关联task的silica值 |
| start_time | string (ISO 8601) | 项目开始时间 | 所有关联repos/task中最早的时间 |
| end_time | string (ISO 8601) | 项目结束时间 | 所有关联repos/task中最晚的时间 |
| start_time_manual | string (ISO 8601) (可null) | 人工校准的项目开始时间 | 人工修正值 |
| end_time_manual | string (ISO 8601) (可null) | 人工校准的项目结束时间 | 人工修正值 |
| upstream_tokens | number | 上行Token总数 | 汇总所有关联task的upstream_tokens |
| downstream_tokens | number | 下行Token总数 | 汇总所有关联task的downstream_tokens |
| cost | number | 总费用（元） | 汇总所有关联task的cost |
| project_ancient_minutes | number | 项目传统开发预估总时长（分钟） | AI分析项目整体代码复杂度预估 [详见](#传统开发时长预估) |
| project_ancient_minutes_reason | string | 传统预估时长说明 | AI生成的分析理由 |
| project_ancient_minutes_manual | number (可null) | 人工校准的传统开发总时长 | 人工修正值 [详见](#人工校准机制) |
| project_ancient_minutes_reason_manual | string (可null) | 人工校准说明 | 人工修正时填写的说明 |
| project_real_process_minutes | number | 项目实际工序耗时（分钟） | 基于task时间序列计算，不含等待间隔 [详见](#项目耗时计算) |
| project_real_process_minutes_reason | string | 工序耗时说明 | 自动生成的计算过程说明 |
| project_real_process_minutes_manual | number (可null) | 人工校准的工序耗时 | 人工修正值 [详见](#人工校准机制) |
| project_real_process_minutes_reason_manual | string (可null) | 人工校准工序耗时说明 | 人工修正时填写的说明 |
| project_real_lead_minutes | number | 项目实际交付周期（分钟） | 端到端时间，含等待间隔 [详见](#项目耗时计算) |
| project_real_lead_minutes_reason | string | 交付周期说明 | 自动生成的计算过程说明 |
| project_real_lead_minutes_manual | number (可null) | 人工校准的交付周期 | 人工修正值 [详见](#人工校准机制) |
| project_real_lead_minutes_reason_manual | string (可null) | 人工校准交付周期说明 | 人工修正时填写的说明 |

#### user_org表

用户与组织的映射关系表，支持多级组织结构。

```json
{
  "user_id": "e20e601d-b1d2-49f1-8f7c-3a447bd170d3",
  "user_name": "15852591920",
  "git_user_name": "zhangsan",
  "git_user_email": "zhangsan@example.com",
  "org1": "研发体系",
  "org2": "平台部",
  "org3": "工具组",
  "org4": "",
  "org5": "",
  "org6": "",
  "org7": "",
  "org8": "",
  "org9": ""
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| user_id | string | 用户唯一标识（主键） | 来自认证系统 |
| user_name | string | 用户名称 | 来自认证系统 |
| git_user_name | string | Git用户名 | 来自Git配置 |
| git_user_email | string | Git用户邮箱 | 来自Git配置 |
| org1~org9 | string | 各级组织名称 | 从上到下的组织层级，org1为最上层（如"研发体系"），org2为下一层（如"平台部"），以此类推。空字符串表示该层级不存在 |

#### user_productivity表

数据库：costrict_stat
表：user_productivity
用途：用户按天的生产力汇总数据。

```json
{
  "user_productivity_id": "a1b2c3d4e5f6_20260321",
  "create_time": "2026-03-21",
  "user_id": "a1b2c3d4e5f6...",
  "user_name": "15852591920",
  "task_ids":["019d3914-eee8-74df-83c6-481b72521f89"],
  "work_dir_ids": ["7fc2b9a3bd58717380e501471c69e48b1cd2ab7b..."],
  "task_diff_lines": 1494,
  "upstream_tokens": 434,
  "downstream_tokens": 4342,
  "cost": 1.43,
  "task_real_minutes": 12.5,
  "task_ancient_minutes": 120.0,
  "task_efficiency_ratio": 0.3142,
  "commit_ids": ["a1b2c3d4e5f6..."],
  "commit_diff_lines": 1494,
  "commit_ancient_minutes": 120.0,
  "commit_real_ai_minutes": 20.5,
  "commit_real_ancient_minutes": 25,
  "commit_real_minutes": 45.5,
  "commit_efficiency_ratio": 0.3142
}
```

| 字段 | 类型 | 含义 | 计算方法/来源 |
|------|------|------|---------------|
| user_productivity_id | string | 记录唯一标识（主键） | 格式：`{user_id}_{create_time}` |
| user_id | string | 用户唯一标识 | 来自关联的task/commit数据 |
| user_name | string | 用户名称 | 来自关联的task/commit数据 |
| task_ids | string[] | 当日关联的task ID列表 | 该用户当天的所有task |
| work_dir_ids | string[] | 当日涉及的工作目录标识列表 | 该用户当天task涉及的work_dir_id |
| task_diff_lines | number | 当日task总变更行数 | 汇总当日所有task的 `diff_lines` |
| upstream_tokens | number | 当日上行Token总数 | 汇总当日所有task的 `upstream_tokens` |
| downstream_tokens | number | 当日下行Token总数 | 汇总当日所有task的 `downstream_tokens` |
| cost | number | 当日总费用（元） | 汇总当日所有task的 `cost` |
| task_real_minutes | number | 当日task实际耗时（分钟） | 汇总当日所有task的 `task_real_minutes` |
| task_ancient_minutes | number | 当日task传统开发预估总时长（分钟） | 汇总当日所有task的 `task_ancient_minutes` |
| task_efficiency_ratio | number | 当日task提效比 | [详见](#efficiency_ratio计算逻辑) |
| commit_ids | string[] | 当日关联的commit ID列表 | 该用户当天的所有commit |
| commit_diff_lines | number | 当日commit总变更行数 | 汇总当日所有commit的 `diff_lines` |
| commit_ancient_minutes | number | 当日commit传统开发预估总时长（分钟） | 汇总当日所有commit的 `commit_ancient_minutes` |
| commit_real_ai_minutes | number | 当日commit中AI辅助实际耗时（分钟） | 汇总当日所有commit的 `commit_real_ai_minutes` |
| commit_real_ancient_minutes | number | 当日commit中非AI部分传统开发时长（分钟） | 汇总当日所有commit的 `commit_real_ancient_minutes` |
| commit_real_minutes | number | 当日commit总实际耗时（分钟） | 汇总当日所有commit的 `commit_real_minutes` |
| commit_efficiency_ratio | number | 当日commit提效比 | [详见](#efficiency_ratio计算逻辑) |
| create_time | string (YYYY-MM-DD) | 统计日期 | 汇总所属的日期 |

## 结构设计

efficiency-dashboard分四个子系统：

- frontend：前端，用户的使用界面，提供效能看板，辅助各级业务管理者进行效能改进。
- backend：后端，效能看板的后端服务，提供各类数据查询API，给前端展示
- indicator: 数据采集上报服务，包括工作在用户工作机上的vscode指标采集上报和cli指标采集上报，工作在云端的指标存储服务。
- kbcli：数据导入程序，把采集到的原始指标数据经导入，转换，计算（ETL）后，保存到backend的数据库。

## 流程设计

### 数据导入逻辑

#### 1. 客户端数据采集

vscode&cli采集：

- 客户端生成task_conversation.jsonl和task_summary.json
- 客户端生成commit数据
- 数据上传到服务器指定目录

#### 2. task数据处理

import-task实现逻辑：

- 扫描task/summary/目录下的所有task_summary.json文件（task_summary.json文件相对task/summary的相对路径称为relSummaryPath）
- 分析task/conversation/目录下和task_summary.json对应的task_conversation.jsonl文件，分析并计算时间序列
- 通过变更代码行数粗略预估传统开发时长
- 在合适时机(比如夜晚空闲)，通过AI提取task描述信息和预估传统开发时长，校准粗略估计的时长
- 删除diff字段后存储到tasks表
- 分析完成后会生成关联的代码行指纹信息文件(.fp),输出到analysed目录的对应位置。该文件可作为标识，表示该文件已经经过分析，下次无需再次处理

#### 3. commit数据处理

import-repo实现逻辑：

- 扫描repo目录下的commit文件
- 删除diff字段后存储到commits表
- 分析关联task并计算silica比例
- 分析完成后会生成关联的代码行指纹信息文件(.fp),输出到analysed目录的对应位置。该文件可作为标识，表示该文件已经经过分析，下次无需再次处理

#### 组织结构导入

import-org:

#### 硅含量计算

silica：

#### 生产力指标计算

efficiency：

### 数据计算逻辑

#### task_real_minutes计算算法 {#task_real_minutes计算算法}

基于conversation时间序列，将连续对话归并为工作片段，累加各片段时长。

**全局配置参数**（backend/config.yaml）：

- `gap_threshold_minutes`：默认30，相邻对话间隔超过此值则视为"断开"
- `extension_minutes`：默认5，每个连续时间片段的末尾追加此延长值

**算法步骤**：

1. 提取所有conversation的`start_time`，过滤掉null值
2. 按`start_time`升序排序
3. 初始化第一个时间片段：`segment_start = times[0]`
4. 遍历`times[1:]`：
   - 计算当前时间与上一个时间的间隔`gap = times[i] - times[i-1]`
   - 如果`gap <= gap_threshold_minutes`：归入当前片段，继续
   - 如果`gap > gap_threshold_minutes`：
     - 结束当前片段：`segment_end = times[i-1] + extension_minutes`
     - 记录当前片段：`[segment_start, segment_end]`
     - 开始新片段：`segment_start = times[i]`
5. 最后一个片段：`segment_end = times[last] + extension_minutes`
6. 累加所有片段的时长：`sum(segment_end - segment_start)`
7. 生成reason字符串描述各片段起止时间

**示例**：

- 对话时间序列：`[10:20:59, 10:24:59, 12:34:59, 12:35:59]`
- gap_threshold=30分钟，extension=5分钟
- 片段1：`10:20:59 ~ 10:29:59`，时长=9.0分钟
- 片段2：`12:34:59 ~ 12:40:59`，时长=6.0分钟
- **task_real_minutes = 9.0 + 6.0 = 15.0分钟**

#### efficiency_ratio计算逻辑 {#efficiency_ratio计算逻辑}

提效比 = 传统开发时长 / 实际耗时 × 100。优先使用人工校准值。

```
effective_ancient = task_ancient_minutes_manual ?? task_ancient_minutes
effective_real = task_real_minutes_manual ?? task_real_minutes

if effective_ancient > 0 AND effective_real > 0:
    efficiency_ratio = (effective_ancient / effective_real) * 100
else:
    efficiency_ratio = null
```

> 该逻辑适用于所有层级的efficiency_ratio计算（task/commit/user/org/project），各层级使用对应的ancient_minutes和real_minutes字段。

#### task贡献度计算 {#task贡献度计算}

导入任务(task)数据时，会计算该任务新增代码的指纹，保存在${task}.fp文件中
导入提交(commit)数据时，会计算该提交新增代码的指纹，保存在${commit}.fp文件中

计算一个任务对提交的贡献度，即：如果一个任务和一个提交相关，计算该提交中新增代码有多少来自该任务的生成。

计算逻辑：
遍历提交的所有新增代码行指纹，查表确认该指纹在那个任务中出现过，统计所有代码行关联的任务。

如果一个提交c有Y个指纹，其中有X个来自任务t，则任务t对提交c的贡献度为 `X / Y`
如果一个某指纹在多个任务中出现过，只算某个任务的贡献。

#### commit关联task计算 {#commit关联task算法}

##### 初步关联条件

同时满足以下条件则建立关联：

- `commit.repo_addr == task.repo_addr`（同一仓库）
- `commit.user_id == task.user_id`（同一用户）
- `task.start_time < commit.commit_time`（task开始早于commit）
- `commit.commit_time < task.start_time + 7days`（commit在task开始后7天内）

##### silica比例分析确认

通过`task贡献度计算`算法，计算silica比例（task贡献了commit多少代码）。

`task_ids_silica` 数组与 `task_ids` 一一对应，每个值表示对应task对commit的代码贡献比例，取值范围 [0, 1]。

#### commit耗时计算 {#commit耗时计算}

基于关联task及其silica比例，拆分commit的实际耗时：

```
commit_real_ai_minutes      = sum(task_ids[i].task_real_minutes × task_ids_silica[i])
commit_real_ancient_minutes = sum(task_ids[i].task_ancient_minutes × (1 - task_ids_silica[i]))
commit_real_minutes         = commit_real_ai_minutes + commit_real_ancient_minutes
```

| 字段 | 含义 | 公式 |
|------|------|------|
| commit_real_ai_minutes | commit中AI辅助部分的实际上机耗时 | `Σ(task_real_minutes[i] × silica[i])` |
| commit_real_ancient_minutes | commit中非AI部分的传统开发预估时长 | `Σ(task_ancient_minutes[i] × (1 - silica[i]))` |
| commit_real_minutes | commit总实际耗时 | `commit_real_ai_minutes + commit_real_ancient_minutes` |

#### 传统开发时长预估 {#传统开发时长预估}

采用两阶段预估策略：

1. **粗略预估**：基于 `diff_lines` 行数快速估算，作为初始值
2. **AI校准**：在合适时机（如夜间空闲），通过AI分析代码复杂度和工作量，生成更准确的预估值及reason说明

AI校准考虑因素包括：涉及的文件数、代码复杂度、是否涉及多个模块/组件、数据库schema变更等。

#### 项目耗时计算 {#项目耗时计算}

projects表中有两种耗时指标：

| 指标 | 含义 | 计算方式 |
|------|------|----------|
| project_real_process_minutes | 实际工序耗时 | 基于task时间序列计算的工序时长，**不含**工作片段间的等待间隔 |
| project_real_lead_minutes | 实际交付周期 | 端到端时间，**含**等待间隔，即 `end_time - start_time` |

#### 人工校准机制 {#人工校准机制}

系统为关键计算字段提供 `_manual` 后缀的人工修正字段和 `_reason_manual` 后缀的说明字段。计算时优先使用 `_manual` 值，若为null则回退到自动计算值。

涉及的字段：

| 自动计算字段 | 人工校准字段 | 说明 |
|-------------|-------------|------|
| task_real_minutes | task_real_minutes_manual | 任务实际耗时 |
| task_ancient_minutes | task_ancient_minutes_manual | 任务传统开发时长 |
| commit_ancient_minutes | commit_ancient_minutes_manual | commit传统开发时长 |
| commit_real_minutes | commit_real_minutes_manual | commit总实际耗时 |
| project_ancient_minutes | project_ancient_minutes_manual | 项目传统开发时长 |
| project_real_process_minutes | project_real_process_minutes_manual | 项目工序耗时 |
| project_real_lead_minutes | project_real_lead_minutes_manual | 项目交付周期 |
| start_time (projects) | start_time_manual | 项目开始时间 |
| end_time (projects) | end_time_manual | 项目结束时间 |

#### path-safe格式化规则 {#path-safe格式化规则}

用于 repo_addr 和 work_dir_id 的标准化，方便URL传递和目录创建。

**转换规则**：原始值 → 转小写 → 非(小写字母/数字/`-`)字符替换为`-` → 去除首尾`-` → 合并连续`-`

| 原始值 | path-safe格式 |
|--------|---------------|
| `https://github.com/zgsm-ai/costrict.git-main` | `https-github-com-zgsm-ai-costrict-git-main` |
| `797e102c29:d:\projects\creditControl\credit-sentinel` | `797e102c29-d-projects-creditcontrol-credit-sentinel` |

#### formatDuration显示规则 {#formatDuration显示规则}

根据分钟数自适应显示时长：

| 分钟范围 | 显示格式 | 示例 |
|----------|----------|------|
| < 60分钟 | X分钟 | 15分钟 |
| 60~480分钟 | X小时Y分钟 | 2小时15分钟 |
| > 480分钟（1人天=8小时=480分钟） | X.X人天 | 1.5人天 |

### 数据展现逻辑

1. **Task详情页面**（/task/task_id）：
   - 展示task的完整信息
   - 支持通过work_dir_id跳转到Work_dir详情页面
   - 支持通过repo_addr跳转到repo详情页面
   - 支持通过user_id跳转到用户详情页面

2. **Commit详情页面**（/commit/commit_id）：
   - 展示commit的完整信息
   - 展示关联的task列表及silica比例
   - 展示AI辅助时长和传统开发时长对比

3. **Repo详情页面**（/repo/path-safe(repo_addr)/path-safe(repo_branch)）：
   - 支持选择不同分支
   - 支持筛选时间段
   - 列出该分支下的所有commits
   - 列出关联的tasks
   - 展示效率评估指标

4. **Project页面**：
   - 支持添加repos过滤条件
   - 支持添加task_ids
   - 展示项目的整体效率指标
   - 支持时间范围筛选

5. **User Productivity页面**：
   - 按天查看用户生产力数据
   - 支持多用户汇总
   - 支持创建虚拟组

6. **Org视图**：
   - 按天展示组织信息
   - 支持日期范围筛选
   - 支持组筛选
   - 支持多级org结构
   - 支持虚拟组

## 接口设计

### 数据库连接信息

- 数据库：costrict_stat
- 用户：postgres
- 密码：1
- 所有表都存储在这个数据库下

### 文件存储规范

1. **task数据**：
   - conversation：`task/conversation/YYYY/MM/DD/$task_id.json`
   - summary：`task/summary/YYYY/MM/DD/$task_id.json`
   - analysed：
     - 汇总信息: `analysed/task/summary/YYYY/MM/DD/$task_id.json`
     - 代码行指纹信息: `analysed/task/summary/YYYY/MM/DD/$task_id.fp`

2. **commit数据**：
   - `repo/$repo_addr/$repo_branch/YYYY/MM/DD/$commit_id.json`
   - 其中repo_addr和repo_branch都经过path-safe格式化
   - 分析产生的commit代码指纹信息保存在`analysed/repo/$repo_addr/$repo_branch/YYYY/MM/DD/$commit_id.fp`

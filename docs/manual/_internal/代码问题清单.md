# 代码问题清单（全量扫描 · 内部）

> 本文件**不交付客户**。是 2026-06-12 编写说明书时对全代码库（前端 23 页 + 后端 handlers + kbcli 算法 + governance + core/utils）端到端考据沉淀的问题清单，去重、分级、带 `文件:行号`。
> 行号基于 `feat/needs-user-search-multi-caliber` 分支当前快照。

## 图例
- 状态：✅ 本次已修 / ⬜ 未处理 / 📄 已文档化（对外白皮书/页面如实写明）/ 🔒 已治理
- 分级：P0 交付级（客户直接看到错误/失效）/ P1 口径正确性 / P2 展示一致性 / P3 安全运维 / L 数据·算法局限（非 bug）

---

## 一、P0 交付级（本次已修）

| # | 问题 | 出处 | 影响 | 状态 |
|---|---|---|---|---|
| P0-1 | 任务页"AI 生成预估/古法重算"按钮调用的 `POST /v2/tasks/estimate-ancient` 已于 commit `c5c10b6` 删除，点击必 404 | `TaskList.tsx`（旧:391）+ `endpoints.ts:206`（已删） | 客户点按钮报错 | ✅ 移除按钮+警告条+caller+死端点+死类型 |
| P0-2 | 仓库/项目 commit·task 行"提效比"用 `古法÷实际×100`（倍数），同页汇总卡用 `(古法−实际)÷实际×100`（提升），同数据行显 400%·卡显 300% | `RepoDetail.tsx:24-35`、`ProjectDetail.tsx:166-167` | 同页两个数自相矛盾 | ✅ RepoDetail 行改提升口径（与卡/后端 `CalcEfficiencyRatio` 一致）；ProjectDetail 见 P1-4 |
| P0-3 | 项目详情仍有"人工调整"，与记忆 `org-project-detail-caliber-v2`"移除人工调整"决策冲突 | `ProjectDetail.tsx`（旧:230-236,518,638-730） | 与下线决策不一致 | ✅ 移除编辑弹窗（按用户决定：仅项目详情；任务/提交保留），保留历史人工值展示 |

---

## 二、P1 口径正确性（影响数字含义）

| # | 问题 | 出处 | 影响/说明 | 状态 |
|---|---|---|---|---|
| P1-1 | **两套提效比口径并存**：V2 融合(小数,RatioPill) vs V1 古法(百分比,PercentPill)，不可横向比较 | 见白皮书 §12 页面→口径表 | 系统级口径分裂 | 📄 白皮书 §12 + 总览警示 + 逐页对照 |
| P1-2 | **V2 工时倒灌 V1**：commit 工时缺失时把 `baseline_fused_work_min`/`total_active_work_corrected_min` 当 V1 ancient/real 按 commit 数平摊 | `backend/db.go:748-759` | 仓库/项目"古法提效比"在缺数 commit 上实为 V2 换算，两口径数据层串味 | ⬜ 设计现状（待评估） |
| P1-3 | **V1 聚合无资格/异常/排除过滤** | `backend/db.go:907-933`、`org_handler_v2.go` | 被 V2 隔离的极端机器生成 commit 在 V1 全额计入，抬高仓库/组织级提效比 | ⬜ |
| P1-4 | 项目**详情**度量卡 `devEfficiencyRatio=古法人天/实际人天`（倍数）vs 项目**列表** `(ancient−real)/real`（提升）——list/detail 口径不一致 | `ProjectDetail.tsx:122-123,166-167` vs `ProjectList.tsx:613-621` | 同一项目列表/详情提效比不同口径。详情内部(卡+行)自洽于倍数，故未盲改 | ⬜ **需决策**：统一到提升（动详情头牌数+PercentPill阈值）或文档化差异 |
| P1-5 | commit 古法行速率 `0.2083 行/分(4.8min/行)` vs task 古法 `2 行/分(0.5min/行)`，差 9.6 倍 | `appconfig/config.go:212` vs `:204` | org 同时汇 task+commit 提效比时口径不齐 | ⬜ |
| P1-6 | 提交间隔法兜底只估 real 不估 ancient | `backend/db.go:774-778` | 该类 commit ancient=0 → `CalcEfficiencyRatio` 返 0 → 提效比恒显 `-` | ⬜ |
| P1-7 | task 古法 ancient 裁剪到 `[max(5,real), 10×real]`，恒 ≥ real | `kbcli/task.go:60-69` | task 提效比理论恒 ≥0，不反映真实负提效 | ⬜ |

---

## 三、P2 展示一致性 / 命名

| # | 问题 | 出处 | 状态 |
|---|---|---|---|
| P2-1 | AI 代码占比 0 值显示不一致：列表 `0.0%`（RatioPill）vs 详情 `-`（fmtPct 把 0 当 -） | `NeedList.tsx` vs `NeedDetail.tsx:95` | 📄 |
| P2-2 | commit 级 `silica` 与 need 级 `ai_code_ratio` 同文案"AI 代码占比"双口径 | `models.go:101` vs `need_aggregate.go:420` | 📄 白皮书§2.10/§12 |
| P2-3 | NeedList "记录时间"列名实为 `dev_start_ts`（开发开始时间） | `NeedList.tsx:398,462` | ⬜ |
| P2-4 | `total_users_v2` 名实不符（取 commit distinct，非 need 维度） | `dashboard_handler_v2.go:118` | ⬜ |
| P2-5 | `total_branchs` 子查询缺日期窗 vs `total_repos` 受日期窗 | `db.go:1500` vs `:1499` | ⬜ |
| P2-6 | 周均提效（TrendCard 算术平均）vs summary 比率（分子分母 SUM 再除）口径不同 | `TrendCard.tsx:38` vs `db.go:1545-1546` | ⬜ |
| P2-7 | `merged_needs` 不带资格过滤、`eligible_needs` 带，AdoptionCard 同列二者关系未点明 | `db.go:1543-1544` | ⬜ |
| P2-8 | UserList 顶部 3 卡用 `allRows` 不随 keyword 过滤变（只"用户数"卡随） | `UserList.tsx:177-182,266-269` | ⬜ |
| P2-9 | UserDetail summary 比率"先求和再推" vs 周行"逐周预存"；趋势图把缺失周画成 0% | `UserDetail.tsx:90` | ⬜ |
| P2-10 | UserDetail Need 表上限 200 / Commit 表上限 100 静默截断 | `user_org_v2_native_handler.go:220,234` | ⬜ |
| P2-11 | ProjectList（按 user_name）/ Detail（按 user_id）userCount 口径不一致 | `project_handler_v2.go:573` vs `:289` | ⬜ |
| P2-12 | RepoList `start_time` 被 `formatLocalTime` 二次解析，东八区显示假 `08:00:00` | `RepoList.tsx:272` | ⬜ |
| P2-13 | RepoDetail 默认分支取 `branches[0]`（字典序）vs 列表"首选分支评分"，直接访问可能对不上 | `RepoDetail.tsx:104` vs `repo_handler_v2.go:450-501` | ⬜ |
| P2-14 | RepoList 每仓库一行（首选分支收敛），其它分支数据不计入该行 | `repo_handler_v2.go:475-501` | 📄（客户问"数偏小"的标准答复） |
| P2-15 | WorkDir 把 `workDirId` 当 `repo_addr` 传（非 `work_dir_id` 列） | `WorkDirDetail.tsx:46` | ⬜ |
| P2-16 | 任务列表"用户名"筛选纯客户端、只过滤当前页，跨页漏 | `TaskList.tsx:188-194` | ⬜ |
| P2-17 | 文件下载 `date` 参数后端不读（冗余） | `endpoints.ts:214` | ⬜ |
| P2-18 | RealtimeQuery 的 ES 源行 `id=0`（行 key 用 request_id 兜底） | `RealtimeQuery.tsx:355` | 📄（外部源特征） |
| P2-19 | Datasource 连接测试失败也是 HTTP 200（须看 body.success） | `Datasources.tsx:70-76` | 📄 |
| P2-20 | 总览硬编码 `¥` 可能与外部 chat `system_currency` 不符 | `HeroSaving.tsx:105` | ⬜ |
| P2-21 | UserGroupDetail 半截功能：能看能删、不能建不能列；删除失败静默无提示；缺数据显 `0.0%` 而非 `-` | `UserGroupDetail.tsx:58-61`、`PercentPill` | ⬜ |

---

## 四、P3 安全 / 权限 / 运维

| # | 问题 | 出处 | 状态 |
|---|---|---|---|
| P3-1 | **全系统无角色门禁**：v2 路由组无 auth 中间件，auth shim 写死 admin。说明书权限不能写"需 platform_admin"，应写"内网无独立角色校验，靠网络隔离 + 上游可选 BasicAuth" | `main.go:112`、`auth_shim_handler.go:12-31` | 📄 |
| P3-2 | repo_addr 内嵌明文 token/密码（294 行实锤） | `sanitize.go:49-72`、`canon.go:23-34` | 🔒 已治理（脱敏 4 表 + canon 剥 token） |
| P3-3 | 治理子 pass 的 startDate/endDate 窗口未实现，每次全量重判 | `governance/pass.go:17-21` TODO | ⬜（性能/增量语义，不影响正确性） |
| P3-4 | 设置 4 页配的是外部 chat-indicator-statistics 服务（非 kbcli）；SyncTasks ≠ kbcli 的 import/efficiency-v2 | `chat_proxy_handler.go:41`、`SyncTasks.tsx` | 📄 |

---

## 五、L 数据采集与算法局限（非 bug，汇报须知）

| # | 项 | 出处 | 状态 |
|---|---|---|---|
| L-1 | verify 阶段恒 0：采集无 command/工具执行事件，fallback 路径 commandText 恒空，verify 分支永不触发，44 项正则形同虚设 | `events.go:96-106`、`stage.go:59-61` | 📄 白皮书§4.2 |
| L-2 | 覆盖判定纯时间维度（temporal-only）：commit 无 touched_files 参与匹配，仅按时间窗 `[start-30,end+60]` | `need_aggregate.go:359-361,493-503` | 📄 白皮书§3.3 |
| L-3 | 零日历 need 被资格否决：采集缺时间侧，非治理可补，资格率偏低主因 | `need_aggregate.go:153-168` | 📄 白皮书§9/§11 |
| L-4 | kNN 有效维度近似 1 维：8 键但取交集，DB 锚点只带 `{log_work_min, ai_code_ratio}`，无归一化 log_work_min 压倒 | `baseline_knn.go:289-299` | 📄 白皮书§5.2（中性表述） |
| L-5 | kNN 锚点人工自报带乐观偏差，×0.499 事后校准（在数据侧 CSV，不在代码） | `数据格式与字段说明_上环境.md:180-182` | 📄 |
| L-6 | **5 项质量护栏已定义阈值、计算逻辑未实现**：churn/revert/伞形/重复/单特征贡献 | `config.go:190-204`、`fusion.go:309-312` | 📄 白皮书 §10.2 标"未启用" |
| L-7 | confidence_level 含死分支 `FeatureDependencyRisk`（无填充，永不触发） | `fusion.go:309-315` | ⬜（内部） |
| L-8 | stage 配置死字段 `gap_threshold_minutes/extension_minutes`（v2 未引用，v2 走 `max_inferred_duration_gap_minutes`） | `config.go:145-150` | ⬜（附录标注） |
| L-9 | 未来时间戳只对超 now 端点 clamp，历史时区偏移不纠 | `need_boundary.go:540-549` | ⬜ |
| L-10 | `ThinkPlanReadMin=0.5` 死系数（think 公式未引用） | `baseline_algo.go:57` | ⬜ |

---

## 六、后端有、前端未用（冗余/遗留字段）

| # | 项 | 出处 |
|---|---|---|
| D-1 | DashboardSummary 大量 V1 字段（total_tasks/total_cost/total_tokens/avg_efficiency_ratio/task_efficiency_ratio/commit_efficiency_ratio…）Overview 未消费 | `types.ts:28-47` |
| D-2 | NeedDetail：`confidence_signals` 整块未消费；baseline `llm_think/exec/verify/spread_work/team_density` 未渲染；need 级质量子指标（churn/duplication/revert/post_gen_delete）未展示 | `needs_v2_handler.go:61,73-81`、`types.ts:127-134` |
| D-3 | UserList：cost/tokens/confidence_limited 未渲染；后端 `userSortFields` 含 cost/tokens/baselineWorkMin 前端不用 | `user_org_v2_native_handler.go:32-35`、`sort.go:317-322` |
| D-4 | Task `efficiency_ratio`（百分比口径）后端返回，两页都不渲染 | `db.go:195-196,231` |
| D-5 | RepoDetail `summary.commit_count/task_count` 未消费（用 commits.length）；后端 per-commit `efficiency_ratio` 弃用 | `repo_handler_v2.go:416-420,395-398` |
| D-6 | UserGroupMember task_diff_lines/tokens/各 minutes 未渲染 | `user_group_handler_v2.go:13-30` |
| D-7 | `time_segments` 死代码（Task/Commit 详情纯线性时间线） | `TaskDetail.tsx:2,5` |

---

## 七、修复优先级建议（供后续排期）

- **建议尽快**：P1-4（项目 list/detail 口径统一，影响汇报横向比较）、P2-3/P2-4（命名误导）、P2-21（用户组半截功能 + 删除失败静默）。
- **建议评估**：P1-2/P1-3/P1-5/P1-6/P1-7（V1 古法口径体系整体——是否随 V2 全量替换 V1，决定要不要逐个修还是整体下线 V1 页面）。
- **可文档化即可**：P2 中 📄 项、L 全部、D 全部（按需补展示或清理冗余）。
- **本次已闭环**：P0-1/P0-2/P0-3。

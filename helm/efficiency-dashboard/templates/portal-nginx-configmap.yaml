{{- if .Values.portal.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "efficiency-dashboard.fullname" . }}-portal-nginx
  labels:
    {{- include "efficiency-dashboard.portal.labels" . | nindent 4 }}
data:
  nginx.conf: |-
{{ .Files.Get "templates/files/nginx.conf" | indent 4 }}
{{- end }}

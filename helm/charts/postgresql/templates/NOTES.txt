Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

PostgreSQL service:

  Internal: {{ include "postgresql.fullname" . }}:{{ .Values.service.port }}
  Database: {{ .Values.auth.database }}
  User: {{ .Values.auth.user }}

Connect to database:

  kubectl run -it --rm --namespace {{ .Release.Namespace }} postgres-client \
    --image=postgres:15-alpine --restart=Never \
    --env="PGPASSWORD={{ .Values.auth.password }}" \
    --command -- psql -h {{ include "postgresql.fullname" . }} -U {{ .Values.auth.user }} -d {{ .Values.auth.database }}

Check logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "postgresql.name" . }} --tail=100 -f

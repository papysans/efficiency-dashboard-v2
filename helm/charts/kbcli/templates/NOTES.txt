Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

kbcli (Data Import CLI):

  Service: {{ include "kbcli.fullname" . }}:{{ .Values.service.port }}

Check logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "kbcli.name" . }} --tail=100 -f

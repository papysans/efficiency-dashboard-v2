apiVersion: v1
kind: Service
metadata:
  name: {{ include "kbcli.fullname" . }}
  labels:
    {{- include "kbcli.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kbcli.selectorLabels" . | nindent 4 }}

Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

Portal (Nginx):

  Internal: {{ include "portal.fullname" . }}:{{ .Values.service.port }}

Get the application URL by running:

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "portal.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:http

Check logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "portal.name" . }} --tail=100 -f
